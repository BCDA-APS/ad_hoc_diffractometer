ad_hoc_diffractometer
=====================

.. py:module:: ad_hoc_diffractometer

.. autoapi-nested-parse::

   ad_hoc_diffractometer — Multi-circle diffractometer geometry and related calculations.



Submodules
----------

.. toctree::
   :maxdepth: 1

   /autoapi/ad_hoc_diffractometer/axes/index
   /autoapi/ad_hoc_diffractometer/constants/index
   /autoapi/ad_hoc_diffractometer/display/index
   /autoapi/ad_hoc_diffractometer/engines/index
   /autoapi/ad_hoc_diffractometer/factories/index
   /autoapi/ad_hoc_diffractometer/forward/index
   /autoapi/ad_hoc_diffractometer/geometry/index
   /autoapi/ad_hoc_diffractometer/lattice/index
   /autoapi/ad_hoc_diffractometer/mode/index
   /autoapi/ad_hoc_diffractometer/orientation/index
   /autoapi/ad_hoc_diffractometer/radiation/index
   /autoapi/ad_hoc_diffractometer/refinement/index
   /autoapi/ad_hoc_diffractometer/reflection/index
   /autoapi/ad_hoc_diffractometer/rotation/index
   /autoapi/ad_hoc_diffractometer/sample/index
   /autoapi/ad_hoc_diffractometer/scan/index
   /autoapi/ad_hoc_diffractometer/stage/index
   /autoapi/ad_hoc_diffractometer/surface/index


Attributes
----------

.. autoapisummary::

   ad_hoc_diffractometer.BASIS_BL
   ad_hoc_diffractometer.BASIS_YOU
   ad_hoc_diffractometer.CRYSTAL_SYSTEMS
   ad_hoc_diffractometer.GEOMETRY_ENTRY_POINT_GROUP
   ad_hoc_diffractometer.HC_KEV_ANGSTROM
   ad_hoc_diffractometer.KAPPA_ALPHA_DEFAULT
   ad_hoc_diffractometer.XHAT
   ad_hoc_diffractometer.XRAY_LINES
   ad_hoc_diffractometer.YHAT
   ad_hoc_diffractometer.ZHAT


Classes
-------

.. autoapisummary::

   ad_hoc_diffractometer.AdHocDiffractometer
   ad_hoc_diffractometer.BisectingMode
   ad_hoc_diffractometer.DiffractionMode
   ad_hoc_diffractometer.FixedAngleMode
   ad_hoc_diffractometer.Lattice
   ad_hoc_diffractometer.ModeDict
   ad_hoc_diffractometer.Reflection
   ad_hoc_diffractometer.ReflectionList
   ad_hoc_diffractometer.Sample
   ad_hoc_diffractometer.SampleDict
   ad_hoc_diffractometer.Stage


Functions
---------

.. autoapisummary::

   ad_hoc_diffractometer.NEAREST_ANGLES
   ad_hoc_diffractometer.Q_mag_to_two_theta
   ad_hoc_diffractometer.Q_to_d
   ad_hoc_diffractometer.Q_to_hkl
   ad_hoc_diffractometer.allclose
   ad_hoc_diffractometer.alpha_f
   ad_hoc_diffractometer.alpha_i
   ad_hoc_diffractometer.angles_to_phi_vector
   ad_hoc_diffractometer.axis_from_physical
   ad_hoc_diffractometer.axis_label
   ad_hoc_diffractometer.b_matrix
   ad_hoc_diffractometer.compute_forward
   ad_hoc_diffractometer.d_to_Q_mag
   ad_hoc_diffractometer.d_to_two_theta
   ad_hoc_diffractometer.energy_to_wavelength
   ad_hoc_diffractometer.fivec
   ad_hoc_diffractometer.fmt
   ad_hoc_diffractometer.fourch
   ad_hoc_diffractometer.fourcv
   ad_hoc_diffractometer.get_geometry
   ad_hoc_diffractometer.get_precision
   ad_hoc_diffractometer.hkl_to_Q
   ad_hoc_diffractometer.hkl_to_d
   ad_hoc_diffractometer.hkl_to_two_theta
   ad_hoc_diffractometer.hkl_trajectory
   ad_hoc_diffractometer.is_evanescent
   ad_hoc_diffractometer.is_specular
   ad_hoc_diffractometer.kappa4ch
   ad_hoc_diffractometer.kappa4cv
   ad_hoc_diffractometer.kappa6c
   ad_hoc_diffractometer.kappa_axis
   ad_hoc_diffractometer.lattice_vectors
   ad_hoc_diffractometer.list_geometries
   ad_hoc_diffractometer.make_geometry
   ad_hoc_diffractometer.parse_axis
   ad_hoc_diffractometer.precision_atol
   ad_hoc_diffractometer.psi_trajectory
   ad_hoc_diffractometer.psic
   ad_hoc_diffractometer.q_components
   ad_hoc_diffractometer.reciprocal_vectors
   ad_hoc_diffractometer.refine_lattice_bl1967
   ad_hoc_diffractometer.refine_lattice_simplex
   ad_hoc_diffractometer.rotation_matrix
   ad_hoc_diffractometer.s2d2
   ad_hoc_diffractometer.set_precision
   ad_hoc_diffractometer.sixc
   ad_hoc_diffractometer.trajectory_plan
   ad_hoc_diffractometer.two_theta_to_Q_mag
   ad_hoc_diffractometer.two_theta_to_d
   ad_hoc_diffractometer.ub_from_one_reflection
   ad_hoc_diffractometer.ub_from_three_reflections_bl1967
   ad_hoc_diffractometer.ub_from_two_reflections_bl1967
   ad_hoc_diffractometer.ub_identity
   ad_hoc_diffractometer.wavelength_to_energy
   ad_hoc_diffractometer.wavelength_to_wavenumber
   ad_hoc_diffractometer.wavenumber_to_wavelength
   ad_hoc_diffractometer.zaxis


Package Contents
----------------

.. py:class:: AdHocDiffractometer(name: str, stages: list[ad_hoc_diffractometer.stage.Stage], basis: dict | None = None, description: str = '', wavelength: float | None = None, kappa_alpha_deg: float | None = None, azimuthal_reference: tuple[float, float, float] | None = None, modes: dict | ad_hoc_diffractometer.mode.ModeDict | None = None, default_mode: str | None = None, cut_points: dict[str, float] | None = None)

   Description of a diffractometer as an ordered collection of rotary stages.

   The geometry is defined by a list of Stage objects.  The stacking order
   (which stage sits on which) is encoded by the parent attribute of each
   Stage: a stage with parent=None sits on the fixed lab frame; all other
   stages sit on the stage named by their parent.

   The class supports arbitrary geometries including:
     - You (1999) psic  4S+2D  six-circle
     - Lohmeier & Vlieg (1993) sixc  six-circle
     - Busing & Levy (1967) fourc  four-circle Eulerian
     - Kappa four-circle

   The basis vectors for the lab frame are specified at construction time,
   allowing the same physical geometry to be described in different
   coordinate conventions.

   :param name: Name of the diffractometer geometry (e.g. 'psic', 'sixc', 'fourc').
   :type name: str
   :param stages: All stages in the geometry.  Order within the list does not determine
                  the stacking; the parent attribute of each Stage does.
   :type stages: list of Stage
   :param basis: Mapping from physical direction names to Cartesian basis vectors.
                 Default is the You (1999) convention:
                     {'vertical': XHAT, 'longitudinal': YHAT, 'lateral': ZHAT}
   :type basis: dict, optional
   :param description: Free-text description of the geometry.
   :type description: str, optional
   :param wavelength: X-ray or neutron wavelength in Å.  Must match the units used for
                      unit cell edge lengths.  Default is None (unset).  Must be > 0
                      if provided.
   :type wavelength: float or None, optional
   :param kappa_alpha_deg: Kappa tilt angle in degrees for kappa geometries (kappa4cv,
                           kappa4ch, kappa6c).  None for non-kappa geometries.  Set by
                           the kappa factory functions; not intended to be changed after
                           construction.
   :type kappa_alpha_deg: float or None, optional
   :param azimuthal_reference: Azimuthal reference direction as Miller indices (h, k, l).  Used
                               by :meth:`psi` to compute the azimuthal angle ψ.  ``None`` (default)
                               means no reference is set.  Must be a non-zero vector.
   :type azimuthal_reference: tuple of float or None, optional
   :param modes: Named diffraction modes available for this geometry.  Keys are
                 mode names (str); values are :class:`~mode.DiffractionMode`
                 instances.  ``None`` (default) means no modes are declared.
   :type modes: dict[str, DiffractionMode] or ModeDict or None, optional
   :param default_mode: Name of the mode that is active at construction time.  Must be a
                        key of ``modes`` if both are supplied.  ``None`` (default) means no
                        active mode is set (``mode_name`` returns ``None``).
   :type default_mode: str or None, optional
   :param cut_points: Geometry-level SPEC #G4 cut-points per stage name.  These are the
                      default cut-points when no mode-level cut-point overrides them.
                      A cut-point C for stage X means the returned angle lies in
                      ``[C, C + 360°)``.  ``None`` is equivalent to an empty dict.
   :type cut_points: dict[str, float] or None, optional

   .. attribute:: sample_stages

      Stages with role='sample', in stacking order (floor first).

      :type: list of Stage

   .. attribute:: detector_stages

      Stages with role='detector', in stacking order (floor first).

      :type: list of Stage

   .. attribute:: wavelength

      Wavelength in Å, or None if not set.

      :type: float or None

   .. attribute:: kappa_alpha_deg

      Kappa tilt angle in degrees, or None for non-kappa geometries.

      :type: float or None

   .. attribute:: azimuthal_reference

      Azimuthal reference vector (h, k, l), or None if not set.

      :type: tuple of float or None

   .. attribute:: modes

      The collection of named modes.  Empty if none were supplied.

      :type: ModeDict

   .. attribute:: mode_name

      Name of the currently active mode, or ``None`` if no mode is set.

      :type: str or None

   .. attribute:: cut_points

      Geometry-level SPEC #G4 cut-points per stage; mirrors SPEC #G4.

      :type: dict[str, float]

   .. attribute:: detector_distance

      Sample-to-detector distance in mm, or ``None`` if not set.

      :type: float or None

   .. attribute:: detector_tilt

      Detector tilt angle in degrees, or ``None`` if not set.

      :type: float or None

   .. attribute:: detector_offset

      In-plane detector offset (dx, dy) in mm, or ``None`` if not set.

      :type: tuple of float or None


   .. py:attribute:: DEFAULT_BASIS


   .. py:method:: add_reflection(name: str, hkl: tuple[float, float, float], angles: dict[str, float], wavelength: float | None = None) -> ad_hoc_diffractometer.reflection.Reflection

      Add a named orienting reflection to the active sample.

      Delegates to ``self.sample.reflections.add()``.  If ``wavelength``
      is None the geometry's current wavelength is inherited.

      :param name: Unique label (e.g. ``"r1"``, ``"Si_111"``).
      :type name: str
      :param hkl: Miller indices (h, k, l).
      :type hkl: tuple of float
      :param angles: Motor angles in degrees keyed by stage name.
      :type angles: dict[str, float]
      :param wavelength: Wavelength in Å.  If None, inherits ``self.wavelength``.
      :type wavelength: float or None

      :rtype: Reflection



   .. py:method:: add_sample(name: str, lattice=None) -> ad_hoc_diffractometer.sample.Sample

      Add a new sample and return it.

      :param name: Unique label for this sample.
      :type name: str
      :param lattice: Crystal lattice.  If None, defaults to cubic a = 1 Å.
      :type lattice: Lattice or None

      :rtype: Sample

      :raises ValueError: If a sample with that name already exists.



   .. py:method:: alpha_f(angles: dict[str, float] | None = None) -> float

      Angle of emergence αf (degrees).

      αf is the angle between the diffracted beam and the sample surface.
      Requires ``wavelength``, ``sample.UB``, and ``surface_normal``
      (or ``azimuthal_reference``) to be set.

      :param angles: Motor angles.  If ``None``, current stage angles are used.
      :type angles: dict[str, float] or None

      :returns: αf in degrees, in [0°, 90°].
      :rtype: float

      .. seealso::

         :py:obj:`alpha_i`
             Angle of incidence.



   .. py:method:: alpha_i(angles: dict[str, float] | None = None) -> float

      Angle of incidence αᵢ (degrees).

      αᵢ is the angle between the incoming beam and the sample surface.
      Requires ``wavelength``, ``sample.UB``, and ``surface_normal``
      (or ``azimuthal_reference``) to be set.

      :param angles: Motor angles.  If ``None``, current stage angles are used.
      :type angles: dict[str, float] or None

      :returns: αᵢ in degrees, in [0°, 90°].
      :rtype: float

      .. seealso::

         :py:obj:`alpha_f`
             Angle of emergence.

      .. rubric:: Examples

      >>> g = zaxis()
      >>> g.wavelength = 1.5406
      >>> g.surface_normal = (0, 0, 1)
      >>> ub_identity(g.sample)
      >>> g.alpha_i({"alpha": 5.0, "Z": 0.0, "delta": 20.0, "gamma": 0.0})
      5.0



   .. py:property:: azimuthal_reference
      :type: tuple[float, float, float] | None


      Azimuthal reference vector as Miller indices (h, k, l), or ``None``.

      The azimuthal reference defines the direction in reciprocal space
      used to set the azimuthal angle ψ = 0.  ψ is zero when this vector
      lies in the scattering plane (the plane containing the incident beam
      and the scattering vector Q).

      Setting to ``None`` clears the reference (ψ becomes undefined).

      Setting to a tuple ``(h, k, l)`` stores it as a tuple of three
      floats.  Any three-element sequence is accepted.

      :param value: Miller indices of the azimuthal reference direction, e.g.
                    ``(0, 0, 1)`` for the surface normal of a (001)-cut crystal.
      :type value: tuple of float or None

      :raises ValueError: If the value is not ``None`` and cannot be interpreted as a
          length-3 sequence of numbers, or if the vector is the zero
          vector.

      .. rubric:: Examples

      >>> g = psic()
      >>> g.azimuthal_reference = (0, 0, 1)   # c-axis surface normal
      >>> g.azimuthal_reference
      (0.0, 0.0, 1.0)
      >>> g.azimuthal_reference = None          # clear reference


   .. py:attribute:: basis


   .. py:method:: check_limits(**angles: float) -> None

      Verify that all supplied angles are within their stage limits.

      :param \*\*angles: Keyword arguments mapping stage name to angle in degrees,
                         e.g. check_limits(mu=10.0, eta=30.0).
      :type \*\*angles: float

      :raises KeyError: If a supplied stage name does not exist in this geometry.
      :raises ValueError: If any supplied angle is outside its stage's limits.  The
          message names every out-of-range stage, its angle, and its
          limits, so all violations are reported in one call.



   .. py:attribute:: cut_points
      :type:  dict[str, float]


   .. py:attribute:: description
      :value: ''



   .. py:property:: detector_distance
      :type: float | None


      Sample-to-detector distance in millimetres, or ``None`` if not set.

      Relevant primarily for area detectors, where the distance is needed
      to convert pixel positions to scattering angles (and thence to Q).
      Point detectors do not require this parameter.

      Must be strictly positive when set.

      :raises ValueError: If the supplied value is not positive.

      .. rubric:: Examples

      >>> g = psic()
      >>> g.detector_distance = 500.0   # 500 mm
      >>> g.detector_distance
      500.0
      >>> g.detector_distance = None    # clear


   .. py:property:: detector_offset
      :type: tuple[float, float] | None


      In-plane detector offset (dx, dy) in millimetres, or ``None`` if not set.

      Describes the displacement of the beam centre from the detector
      centre in the detector plane.  ``dx`` is the horizontal offset and
      ``dy`` is the vertical offset, both in millimetres.  A zero offset
      means the beam strikes the geometric centre of the detector.

      Must be a two-element sequence of real numbers.

      :raises ValueError: If the supplied value is not a length-2 sequence of numbers.

      .. rubric:: Examples

      >>> g = psic()
      >>> g.detector_offset = (2.5, -1.0)   # 2.5 mm right, 1.0 mm down
      >>> g.detector_offset
      (2.5, -1.0)
      >>> g.detector_offset = None           # clear


   .. py:method:: detector_rotation_matrix() -> numpy.ndarray

      Compute the total detector rotation matrix in stacking order.

      :returns: **D**
      :rtype: numpy.ndarray, shape (3, 3)



   .. py:attribute:: detector_stages
      :value: []



   .. py:property:: detector_tilt
      :type: float | None


      Detector tilt angle in degrees, or ``None`` if not set.

      A non-zero tilt indicates that the detector plane is not perfectly
      perpendicular to the diffracted beam at the nominal detector position.
      The tilt is a rotation of the detector face about an axis parallel
      to the detector plane (a pitch/roll correction for non-ideal
      alignment).

      Any real number is accepted (positive or negative; modulo 360° is
      not applied).

      .. rubric:: Examples

      >>> g = psic()
      >>> g.detector_tilt = 0.3    # 0.3° tilt
      >>> g.detector_tilt
      0.3
      >>> g.detector_tilt = None   # clear


   .. py:property:: energy
      :type: float | None


      Radiation energy in units of :attr:`energy_units`, derived from wavelength.

      For X-ray geometries (default): E (keV) = hc / λ = 12.39842 / λ (Å).

      Returns ``None`` when ``wavelength`` is not set.  Setting ``energy``
      updates ``wavelength`` accordingly.  The value must be in the units
      returned by :attr:`energy_units`.

      :raises ValueError: If set to a non-positive value.

      .. rubric:: Examples

      >>> import ad_hoc_diffractometer as ahd
      >>> g = ahd.fourcv()
      >>> g.wavelength = 1.5406
      >>> round(g.energy, 4)
      8.0479
      >>> g.energy = 8.048
      >>> round(g.wavelength, 4)
      1.5405


   .. py:property:: energy_units
      :type: str


      Units of the :attr:`energy` property for this geometry.

      Returns ``"keV"`` for X-ray geometries (the default).  Will return
      ``"meV"`` for neutron geometries once ``source_type`` is set
      (see issue #8).

      :rtype: str


   .. py:method:: forward(h: float, k: float, l: float) -> list[dict[str, float]]

      Compute all valid motor-angle solutions for the reflection (h, k, l).

      This is the *forward* diffraction calculation: given a reciprocal-
      lattice point, compute the motor angles that satisfy the Bragg
      condition under the active diffraction mode.

      Algorithm (delegated to :mod:`.forward`):

      1. Compute ``Q_phi = UB @ (h, k, l)`` — the target scattering vector
         in the phi frame.
      2. Apply Bragg's law to find the detector angle (2θ).
      3. Apply mode constraints (frozen angles, bisecting condition) to
         determine the remaining free stage angles.
      4. Return all valid solutions, filtered by stage limits.

      :param h: Miller indices of the target reflection.
      :type h: float
      :param k: Miller indices of the target reflection.
      :type k: float
      :param l: Miller indices of the target reflection.
      :type l: float

      :returns: Each element is a complete set of motor angles (all stage names
                as keys, values in degrees) that satisfies the Bragg condition
                under the active mode.  May be empty if no valid solution exists
                within limits.
      :rtype: list of dict[str, float]

      :raises ValueError: If ``self.wavelength`` is None.
      :raises ValueError: If ``self.sample.UB`` is None.
      :raises ValueError: If (h, k, l) == (0, 0, 0).
      :raises ValueError: If the requested |Q| exceeds the Ewald sphere.
      :raises NotImplementedError: If no active mode is set or the mode type is not supported.

      .. seealso::

         :py:obj:`inverse`
             Convert motor angles to (h, k, l).

      .. rubric:: Examples

      >>> import ad_hoc_diffractometer as ahd
      >>> g = ahd.fourcv()
      >>> g.wavelength = 1.5406
      >>> ahd.ub_identity(g.sample)
      >>> solutions = g.forward(1, 0, 0)
      >>> len(solutions) > 0
      True
      >>> sorted(solutions[0].keys())
      ['chi', 'omega', 'phi', 'ttheta']



   .. py:method:: from_dict(d: dict) -> AdHocDiffractometer
      :classmethod:


      Reconstruct an :class:`AdHocDiffractometer` from a dict produced by
      :meth:`to_dict`.

      :param d: Dict as returned by :meth:`to_dict`.  The ``"_meta"`` key is
                read but not validated; version mismatches do not raise.
      :type d: dict

      :returns: A fully-configured geometry with all stages, samples, and
                settings restored.
      :rtype: AdHocDiffractometer

      .. rubric:: Notes

      Stages are reconstructed from the stored axis, role, parent, limits,
      and angle values.  The stage parent graph is rebuilt from the stored
      ``"parent"`` field (a name string or None).

      Samples are restored via :meth:`Sample.from_dict`.  The active
      sample is restored by name.

      The factory-function identity (e.g. ``psic``, ``fourcv``) is **not**
      stored; ``from_dict`` always returns a plain
      :class:`AdHocDiffractometer` instance.  If you need the factory
      function, look it up via ``get_geometry(d["name"])``.

      .. rubric:: Examples

      >>> import ad_hoc_diffractometer as ahd
      >>> g = ahd.fourcv()
      >>> g.wavelength = 1.5406
      >>> g2 = ahd.AdHocDiffractometer.from_dict(g.to_dict())
      >>> g2.name
      'fourcv'
      >>> g2.wavelength
      1.5406



   .. py:property:: inclination_matrix
      :type: numpy.ndarray


      3×3 rotation matrix describing the diffractometer inclination
      relative to the incident beam direction.

      When the entire diffractometer is mounted at a non-zero angle with
      respect to the incident beam (e.g. tilted for grazing-incidence
      geometry or non-standard instrument mounting), this matrix encodes
      that orientation.

      The matrix **R** is applied as follows: the effective incident-beam
      direction in the diffractometer coordinate frame is
      ``R.T @ ŷ`` rather than ``ŷ``.  A zero inclination (identity matrix)
      reproduces the standard geometry exactly.

      The matrix must be a proper rotation (orthonormal, det = +1).

      Default: ``numpy.eye(3)`` (no inclination).

      :raises ValueError: If the supplied array is not shape (3, 3), not orthonormal
          within 1e-8, or has det ≠ +1.

      .. seealso::

         :py:obj:`set_inclination`
             Set the inclination from an axis and angle.

      .. rubric:: Examples

      >>> import numpy as np
      >>> g = psic()
      >>> g.inclination_matrix  # default: identity
      array([[1., 0., 0.],
             [0., 1., 0.],
             [0., 0., 1.]])
      >>> g.set_inclination(axis=[0, 0, 1], angle_deg=2.0)  # 2° about z
      >>> np.round(g.inclination_matrix, 4)
      array([[ 0.9994,  0.0349,  0.    ],
             [-0.0349,  0.9994,  0.    ],
             [ 0.    ,  0.    ,  1.    ]])


   .. py:method:: inverse(angles: dict[str, float]) -> tuple[float, float, float]

      Convert a set of motor angles to Miller indices (h, k, l).

      This is the *inverse* diffraction calculation: given where the
      diffractometer motors are pointing, compute the reciprocal-lattice
      point being measured.

      Algorithm:

      1. Compute ``Q_phi`` from the motor angles via
         :func:`angles_to_phi_vector`.
      2. Solve ``UB @ hkl = Q_phi`` for ``hkl``, i.e.
         ``hkl = UB⁻¹ @ Q_phi``.

      :param angles: Motor angles in degrees, keyed by stage name.  All stages
                     present in the geometry may be supplied; stages not supplied
                     keep their current ``angle`` attribute.
      :type angles: dict[str, float]

      :returns: **hkl** -- Miller indices ``(h, k, l)`` corresponding to the supplied
                motor angles.
      :rtype: tuple of float

      :raises KeyError: If a supplied stage name does not exist in the geometry.
      :raises ValueError: If ``self.wavelength`` is None.
      :raises ValueError: If the active sample has no UB matrix set
          (``sample.UB is None``).
      :raises numpy.linalg.LinAlgError: If the UB matrix is singular and cannot be inverted.

      .. rubric:: Examples

      >>> import ad_hoc_diffractometer as ahd
      >>> g = ahd.psic()
      >>> g.wavelength = 1.5406
      >>> ahd.ub_identity(g.sample)          # set UB = B (cubic a=1)
      >>> hkl = g.inverse(
      ...     {"mu": 0, "eta": 20.97, "chi": 90, "phi": 0,
      ...      "nu": 0, "delta": 41.94}
      ... )



   .. py:method:: is_evanescent(angles: dict[str, float] | None = None, critical_angle_deg: float | None = None) -> bool

      Return True when αᵢ < ``critical_angle_deg`` (evanescent regime).

      :param angles:
      :type angles: dict[str, float] or None
      :param critical_angle_deg: Critical angle for total external reflection in degrees.
                                 Must be supplied (material-dependent; not stored on geometry).
      :type critical_angle_deg: float

      :rtype: bool

      :raises ValueError: If ``critical_angle_deg`` is None.



   .. py:method:: is_specular(angles: dict[str, float] | None = None, atol: float = 0.01) -> bool

      Return True when αᵢ ≈ αf within ``atol`` degrees.

      :param angles:
      :type angles: dict[str, float] or None
      :param atol: Tolerance in degrees (default 0.01°).
      :type atol: float

      :rtype: bool



   .. py:property:: kappa_alpha_deg
      :type: float | None


      Kappa tilt angle in degrees, or None for non-kappa geometries.

      This is the angle between the kappa rotation axis and the vertical
      axis (toward the lateral axis).  Typical value: 50 deg.
      Set by kappa factory functions (kappa4cv, kappa4ch, kappa6c).


   .. py:property:: mode
      :type: ad_hoc_diffractometer.mode.DiffractionMode | None


      The currently active :class:`~mode.DiffractionMode`, or ``None``.

      Equivalent to ``self.modes[self.mode_name]`` when a mode is
      active; ``None`` when :attr:`mode_name` is ``None``.

      :rtype: DiffractionMode or None


   .. py:property:: mode_name
      :type: str | None


      Name of the currently active diffraction mode, or ``None``.

      Setting this property switches the active mode.  The name must
      be a key of :attr:`modes`.

      :rtype: str or None

      :raises ValueError: If the supplied name is not in :attr:`modes`.


   .. py:property:: modes
      :type: ad_hoc_diffractometer.mode.ModeDict


      The collection of named diffraction modes for this geometry.

      A ``ModeDict`` mapping mode name (str) to
      :class:`~mode.DiffractionMode` instance.  Empty when no modes
      have been declared.

      :rtype: ModeDict


   .. py:attribute:: name


   .. py:method:: pa(print: bool = True) -> str

      Verbose parameter listing (the SPEC ``pa`` command).

      Shows the geometry name, the two designated orienting reflections
      (if set), lattice constants in real and reciprocal space, the
      azimuthal reference vector, and the wavelength.

      :param print: If ``True`` (default), the string is printed to stdout before
                    being returned.  Pass ``print=False`` to suppress output and
                    capture the string instead (useful in tests and logging).
      :type print: bool, optional

      :returns: Multi-line parameter string.
      :rtype: str

      .. rubric:: Examples

      >>> import ad_hoc_diffractometer as ahd
      >>> g = ahd.fourcv()
      >>> g.pa()                          # prints and returns
      Geometry: fourcv
      ...
      >>> out = g.pa(print=False)         # capture without printing
      >>> "Geometry" in out
      True

      .. rubric:: References

      Align4Pete.log — ``pa`` command outputs, 7-ID-C fourc session,
      Dec 2020.



   .. py:method:: psi(angles: dict[str, float] | None = None) -> float

      Compute the azimuthal angle ψ (psi) for the given motor angles.

      ψ is the rotation of the azimuthal reference vector about the
      scattering vector Q, measured from the zero-ψ position.  ψ = 0
      when the reference vector lies in the scattering plane (the plane
      containing the incident beam direction and Q).

      Algorithm (You 1999, eqs. 10-11):

      1. Compute ``Q_phi`` from the motor angles via
         :func:`angles_to_phi_vector`.
      2. Express the reference direction in the phi frame:
         ``n_phi = UB @ n_hkl``  (maps reciprocal-lattice direction to
         phi frame in the same units as Q).
      3. Project both ``n_phi`` and the incident-beam direction
         ``y_hat`` (the ``'longitudinal'`` basis vector) onto the plane
         perpendicular to ``Q_phi``.
      4. ψ = ``atan2(Q_hat · (y_perp × n_perp), y_perp · n_perp)``
         where all vectors are unit vectors in the perpendicular plane.

      :param angles: Motor angles in degrees, keyed by stage name.  If ``None``
                     (default), the current stage angles are used.
      :type angles: dict[str, float] or None

      :returns: **psi** -- Azimuthal angle in degrees, in the range (-180, 180].
      :rtype: float

      :raises ValueError: If ``self.wavelength`` is None.
      :raises ValueError: If ``self.sample.UB`` is None.
      :raises ValueError: If ``self.azimuthal_reference`` is None (no reference set).
      :raises ValueError: If the reference vector is parallel to Q (ψ is undefined).

      .. rubric:: Notes

      The zero-ψ reference direction is the component of the incident
      beam (``'longitudinal'`` basis vector) perpendicular to Q.  This
      matches the SPEC and You (1999) convention: ψ = 0 when the
      reference vector lies in the scattering plane.

      The incident beam direction is taken from the geometry's basis
      dict under the key ``'longitudinal'``.  For You (1999) geometries
      this is YHAT; for Busing & Levy geometries it is also YHAT.

      .. rubric:: Examples

      >>> import ad_hoc_diffractometer as ahd
      >>> g = ahd.fourcv()
      >>> g.wavelength = 1.5406
      >>> g.azimuthal_reference = (0, 0, 1)
      >>> # ... set UB, move motors ...
      >>> psi = g.psi()

      .. rubric:: References

      You, J. Appl. Cryst. 32, 614-623 (1999), eqs. 10-11.



   .. py:method:: q_components(angles: dict[str, float] | None = None) -> dict[str, float]

      Decompose Q into components parallel and perpendicular to the surface.

      :param angles: Motor angles.  If ``None``, current stage angles are used.
      :type angles: dict[str, float] or None

      :returns: * dict with keys ``"Q_perp"``, ``"Q_par"``, ``"Q_perp_signed"``,
                * ``"Q_total"`` — all in Å⁻¹.

      .. seealso:: :py:obj:`alpha_i`, :py:obj:`alpha_f`



   .. py:attribute:: reflections
      :type:  ad_hoc_diffractometer.reflection.ReflectionList


   .. py:method:: remove_sample(name: str) -> None

      Remove a sample from the dict.

      The active sample cannot be removed; select a different sample
      first.  Clears ``sample.parent`` on the removed sample so that any
      external reference to it knows it is detached.  Delegates to
      SampleDict which enforces the active-sample invariant.

      :raises KeyError: If the sample does not exist.
      :raises ValueError: If the sample is the currently active sample.



   .. py:property:: sample
      :type: ad_hoc_diffractometer.sample.Sample


      The currently active Sample.


   .. py:method:: sample_rotation_matrix() -> numpy.ndarray

      Compute the total sample rotation matrix Z = R_floor * ... * R_top.

      Stages are applied in stacking order: the floor-most stage is applied
      first (leftmost in the product), the innermost stage last.

      :returns: **Z**
      :rtype: numpy.ndarray, shape (3, 3)



   .. py:attribute:: sample_stages
      :value: []



   .. py:property:: samples
      :type: ad_hoc_diffractometer.sample.SampleDict


      The guarded SampleDict of all samples.

      Supports read-only iteration (``for name in g.samples``, ``g.samples[name]``).
      Mutation goes through ``add_sample()`` / ``remove_sample()`` to keep
      invariants intact.


   .. py:method:: set_angle(name: str, angle_deg: float) -> None

      Set the angle of a named stage in degrees.



   .. py:method:: set_inclination(axis: numpy.ndarray | list[float], angle_deg: float) -> None

      Set the inclination matrix from a rotation axis and angle.

      Convenience wrapper around :attr:`inclination_matrix` that uses the
      :func:`~.rotation.rotation_matrix` function to build R from an axis
      vector and an angle in degrees.

      A zero angle resets the inclination to the identity matrix.

      :param axis: Rotation axis (need not be a unit vector; it is normalised
                   internally).
      :type axis: array-like, shape (3,)
      :param angle_deg: Rotation angle in degrees.
      :type angle_deg: float

      :raises ValueError: If ``axis`` is the zero vector.

      .. rubric:: Examples

      >>> g = psic()
      >>> g.set_inclination(axis=[0, 0, 1], angle_deg=2.0)
      >>> g.set_inclination(axis=[1, 0, 0], angle_deg=0.0)  # reset



   .. py:method:: stage(name: str) -> ad_hoc_diffractometer.stage.Stage

      Return the Stage with the given name.



   .. py:method:: summary() -> None

      Print a human-readable summary of the geometry.



   .. py:property:: surface_normal
      :type: tuple[float, float, float] | None


      Surface normal direction as Miller indices (h, k, l), or ``None``.

      The surface normal defines the direction perpendicular to the sample
      surface in reciprocal space.  It is used by the surface geometry
      calculations (:meth:`alpha_i`, :meth:`alpha_f`, :meth:`q_components`,
      :meth:`is_specular`, :meth:`is_evanescent`).

      When ``None``, the surface calculations fall back to
      :attr:`azimuthal_reference` if that is set.

      Setting this to ``None`` clears the surface normal.
      Setting to a non-zero (h, k, l) 3-tuple stores it.

      :raises ValueError: If the supplied value is not a length-3 sequence of numbers,
          or is the zero vector (0, 0, 0).

      .. rubric:: Examples

      >>> g = psic()
      >>> g.surface_normal = (0, 0, 1)   # c-axis surface normal
      >>> g.surface_normal
      (0.0, 0.0, 1.0)
      >>> g.surface_normal = None        # clear


   .. py:method:: to_dict() -> dict

      Export the complete diffractometer configuration as a
      JSON-serialisable ``dict``.

      The returned dict captures all geometry state: stage definitions,
      current motor angles, all samples (including their lattices,
      reflections, and U/UB matrices), the active sample name,
      wavelength, azimuthal reference, and identifying metadata.

      :returns: JSON-serialisable mapping with the following top-level keys:

                ``"_meta"``
                    Sub-dict with ``"software"`` (``"ad_hoc_diffractometer"``),
                    ``"version"`` (package version string), and
                    ``"created"`` (ISO-8601 UTC timestamp).
                ``"name"``
                    Geometry name (str).
                ``"description"``
                    Free-text description (str).
                ``"wavelength"``
                    Wavelength in Å (float or None).
                ``"kappa_alpha_deg"``
                    Kappa tilt angle in degrees (float or None).
                ``"azimuthal_reference"``
                    Miller indices [h, k, l] (list of 3 float, or None).
                ``"basis"``
                    Dict mapping physical direction names to [x, y, z] lists.
                ``"stages"``
                    List of stage dicts (name, axis, role, parent, angle,
                    limits).
                ``"active_sample"``
                    Name of the currently active sample (str).
                ``"samples"``
                    Dict mapping sample names to sample dicts.
      :rtype: dict

      .. rubric:: Examples

      >>> import json, ad_hoc_diffractometer as ahd
      >>> g = ahd.fourcv()
      >>> g.wavelength = 1.5406
      >>> d = g.to_dict()
      >>> assert json.dumps(d)          # must be JSON-serialisable
      >>> d["name"]
      'fourcv'
      >>> d["wavelength"]
      1.5406



   .. py:property:: wavelength
      :type: float | None


      Wavelength in Å, or None if not set.


   .. py:property:: wavenumber
      :type: float | None


      Wave number k in Å⁻¹, derived from wavelength.

      k (Å⁻¹) = 2π / λ (Å)

      Returns ``None`` when ``wavelength`` is not set.  Setting
      ``wavenumber`` updates ``wavelength`` accordingly.

      :raises ValueError: If set to a non-positive value.

      .. rubric:: Examples

      >>> import ad_hoc_diffractometer as ahd
      >>> g = ahd.fourcv()
      >>> g.wavelength = 1.5406
      >>> round(g.wavenumber, 4)
      4.0774
      >>> g.wavenumber = 4.0
      >>> round(g.wavelength, 4)
      1.5708


   .. py:method:: wh(print: bool = True) -> str

      Terse one-screen status string (the SPEC ``wh`` command).

      Shows the current reciprocal-space position (H K L), azimuthal
      angle ψ, wavelength, and a motor-angle table with SPEC-style
      column names.  Graceful fallback text is shown for any field
      that cannot be computed (e.g. no UB matrix or no wavelength).

      :param print: If ``True`` (default), the string is printed to stdout before
                    being returned.  Pass ``print=False`` to suppress output and
                    capture the string instead (useful in tests and logging).
      :type print: bool, optional

      :returns: Multi-line status string.
      :rtype: str

      .. rubric:: Examples

      >>> import ad_hoc_diffractometer as ahd
      >>> g = ahd.fourcv()
      >>> g.wavelength = 1.5406
      >>> g.wh()                          # prints and returns
      H K L = not available
      Psi = not available
      Lambda = 1.5406
      <BLANKLINE>
        TwoTheta     Theta       Chi       Phi
           0.000     0.000     0.000     0.000
      >>> out = g.wh(print=False)         # capture without printing
      >>> "Lambda" in out
      True

      .. rubric:: References

      Align4Pete.log — ``wh`` command outputs, 7-ID-C fourc session,
      Dec 2020.



.. py:data:: BASIS_BL

.. py:data:: BASIS_YOU

.. py:class:: BisectingMode(sample_stage: str, detector_stage: str, frozen_angles: dict[str, float] | None = None, cut_points: dict[str, float] | None = None)

   Bases: :py:obj:`DiffractionMode`


   Bisecting geometry: the sample rotation is set to half the detector angle.

   In the four-circle / psic convention, "bisecting" means that the
   incident and diffracted beam make equal angles with the diffracting
   planes.  Concretely:

   - For ``fourcv`` / ``fourch``: ``omega = ttheta / 2``
   - For ``psic``: ``eta = delta / 2`` (with ``mu = nu = 0`` by convention)

   This mode records the convention via ``sample_stage`` (the stage that is
   driven to half the detector angle) and ``detector_stage`` (the reference
   detector stage).

   :param sample_stage: Name of the sample rotation stage driven to half the detector angle
                        (e.g. ``"omega"`` for fourcv, ``"eta"`` for psic).
   :type sample_stage: str
   :param detector_stage: Name of the detector stage whose angle determines the constraint
                          (e.g. ``"ttheta"`` for fourcv, ``"delta"`` for psic).
   :type detector_stage: str
   :param frozen_angles: Additional stages held fixed (e.g. ``{"mu": 0.0, "nu": 0.0}`` for psic).
   :type frozen_angles: dict[str, float] or None
   :param cut_points: Branch-selection cut-points (see :class:`DiffractionMode`).
   :type cut_points: dict[str, float] or None

   .. rubric:: Examples

   >>> mode = BisectingMode(sample_stage="omega", detector_stage="ttheta")
   >>> mode.constrained_stages
   ['omega']
   >>> mode_psic = BisectingMode(
   ...     sample_stage="eta",
   ...     detector_stage="delta",
   ...     frozen_angles={"mu": 0.0, "nu": 0.0},
   ... )
   >>> mode_psic.constrained_stages
   ['eta', 'mu', 'nu']


   .. py:property:: constrained_stages
      :type: list[str]


      Stages constrained by the bisecting condition.

      Always includes ``sample_stage``, plus any additional stages in
      ``frozen_angles``.


   .. py:property:: detector_stage
      :type: str


      The detector stage that determines the bisecting constraint.


   .. py:property:: sample_stage
      :type: str


      The sample rotation stage driven to half the detector angle.


.. py:data:: CRYSTAL_SYSTEMS
   :value: ['cubic', 'tetragonal', 'orthorhombic', 'hexagonal', 'trigonal', 'monoclinic', 'triclinic']


.. py:class:: DiffractionMode(frozen_angles: dict[str, float] | None = None, cut_points: dict[str, float] | None = None)

   Bases: :py:obj:`abc.ABC`


   Abstract base class for diffraction modes.

   A mode specifies:
   - which stages are frozen (held at a fixed angle during the calculation)
   - which stages are free (computed by the forward calculation)
   - per-stage cut-points for branch selection (SPEC #G4)

   Subclasses must implement :meth:`constrain` and :meth:`constrained_stages`.

   :param frozen_angles: Mapping of stage name -> frozen angle (degrees).  Stages in this
                         dict are held at the given angle during diffraction calculations.
                         ``None`` is equivalent to an empty dict (no frozen angles).
   :type frozen_angles: dict[str, float] or None
   :param cut_points: Mapping of stage name -> cut-point angle (degrees).  The returned
                      solution angle for that stage lies in ``[cut, cut + 360)``.
                      ``None`` is equivalent to an empty dict (no cut-points set, i.e.
                      the default branch [-180, 180) is used).
   :type cut_points: dict[str, float] or None

   .. attribute:: frozen_angles

      Stages held fixed in this mode; mirrors SPEC #G0.

      :type: dict[str, float]

   .. attribute:: cut_points

      Branch selection cut-points per stage; mirrors SPEC #G4.

      :type: dict[str, float]


   .. py:method:: apply_cut_point(stage_name: str, angle_deg: float) -> float

      Apply the cut-point for *stage_name* to *angle_deg*.

      If no cut-point is set for this stage, the angle is returned
      unchanged.

      A cut-point C means the returned angle lies in ``[C, C + 360)``.

      :param stage_name: Stage to apply the cut-point to.
      :type stage_name: str
      :param angle_deg: Angle to normalise (degrees).
      :type angle_deg: float

      :returns: Angle normalised to the cut-point interval.
      :rtype: float



   .. py:property:: constrained_stages
      :type: list[str]

      :abstractmethod:


      Return the list of stage names that are constrained (not free) in this mode.

      Constrained stages include both:
      - frozen stages (held at a fixed angle)
      - stages whose angle is determined by a relationship to another stage
        (e.g. bisecting: omega = ttheta/2)

      :rtype: list of str


   .. py:attribute:: cut_points
      :type:  dict[str, float]


   .. py:attribute:: frozen_angles
      :type:  dict[str, float]


.. py:class:: FixedAngleMode(stage: str, value: float, cut_points: dict[str, float] | None = None)

   Bases: :py:obj:`DiffractionMode`


   Mode that constrains one stage to a fixed angle.

   The stage named ``stage`` is held at ``value`` degrees for all
   diffraction calculations performed in this mode.

   :param stage: Name of the stage to freeze.
   :type stage: str
   :param value: Angle (degrees) at which to freeze the stage.
   :type value: float
   :param cut_points: Branch-selection cut-points (see :class:`DiffractionMode`).
   :type cut_points: dict[str, float] or None

   .. rubric:: Examples

   >>> mode = FixedAngleMode(stage="chi", value=90.0)
   >>> mode.frozen_angles
   {'chi': 90.0}
   >>> mode.constrained_stages
   ['chi']


   .. py:property:: constrained_stages
      :type: list[str]


      just the one frozen stage.

      :type: Stages constrained by this mode


.. py:data:: GEOMETRY_ENTRY_POINT_GROUP
   :value: 'ad_hoc_diffractometer.geometries'


.. py:data:: HC_KEV_ANGSTROM
   :type:  float
   :value: 12.39842


.. py:data:: KAPPA_ALPHA_DEFAULT
   :value: 50.0


.. py:class:: Lattice(a: float = 1.0, b: float = _UNSET, c: float = _UNSET, alpha: float = _UNSET, beta: float = _UNSET, gamma: float = _UNSET, precision: int | None = None)

   A crystal lattice described by up to six parameters (a, b, c, α, β, γ).

   The crystal system is deduced automatically from the parameters supplied.
   Missing parameters are filled in according to the symmetry constraints of
   the deduced system.  The default (no arguments) is a cubic lattice with
   a = 1 Å.

   All parameters are validated on construction and on every change:
     - Lengths (a, b, c) must be strictly positive.
     - Angles (alpha, beta, gamma) must be in (1, 179) degrees.
     - The combination must produce a non-degenerate unit cell.

   Cartesian lattice vectors, reciprocal lattice vectors, and the B matrix
   are computed lazily: calculated on first access and cached until any
   lattice parameter changes.

   :param a: Lattice parameter a in Angstroms.  Default 1.0.
   :type a: float, optional
   :param b: Lattice parameter b in Angstroms.
   :type b: float, optional
   :param c: Lattice parameter c in Angstroms.
   :type c: float, optional
   :param alpha: Angle between b and c axes in degrees.
   :type alpha: float, optional
   :param beta: Angle between a and c axes in degrees.
   :type beta: float, optional
   :param gamma: Angle between a and b axes in degrees.
   :type gamma: float, optional
   :param Properties (read/write):
   :param -----------------------:
   :param a:
   :type a: float — lattice parameters in Angstroms
   :param b:
   :type b: float — lattice parameters in Angstroms
   :param c:
   :type c: float — lattice parameters in Angstroms
   :param alpha:
   :type alpha: float — lattice angles in degrees
   :param beta:
   :type beta: float — lattice angles in degrees
   :param gamma:
   :type gamma: float — lattice angles in degrees
   :param precision: package-level default from display.get_precision()
   :type precision: int or None — decimal places for display; None uses the
   :param Properties (read-only:
   :param lazy):
   :param ----------------------------:
   :param system:
   :type system: str — deduced crystal system
   :param cartesian_lattice_vectors:
   :type cartesian_lattice_vectors: tuple(a1, a2, a3) — direct lattice vectors (Å)
   :param reciprocal_lattice_vectors:
   :type reciprocal_lattice_vectors: tuple(b1, b2, b3) — reciprocal vectors (Å⁻¹, with 2π)
   :param B:
   :type B: ndarray (3,3) — B matrix (Å⁻¹, no 2π)

   .. rubric:: Examples

   >>> Lattice()                                     # cubic, a=1 Å
   >>> Lattice(a=5.0)                                # cubic, a=5 Å
   >>> Lattice(a=3.0, c=6.0)                         # tetragonal
   >>> Lattice(a=4.785, c=12.991, gamma=120.0)       # hexagonal
   >>> Lattice(a=5.0, alpha=60.0)                    # trigonal
   >>> Lattice(a=2.0, b=3.0, c=4.0)                 # orthorhombic
   >>> Lattice(a=5.0, b=6.0, c=7.0, beta=110.0)     # monoclinic
   >>> Lattice(a=3, b=4, c=5, alpha=80, beta=85, gamma=95)  # triclinic
   >>> lat = Lattice(a=4.785, c=12.991, gamma=120.0)
   >>> lat.precision = 3
   >>> print(lat)   # shows 3 decimal places


   .. py:property:: B
      :type: numpy.ndarray


      B matrix in Å⁻¹ (with 2π factor, Busing & Levy 1967 / SPEC convention).

      Transforms Miller indices **h** = (h, k, l) to the scattering vector
      in Cartesian crystal-frame coordinates:

          Q_c = B @ h

      ``|B @ h| = 2π / d_hkl``.  The orthogonality condition is
      ``bᵢ · aⱼ = 2π δᵢⱼ``.

      This follows the Busing & Levy (1967) eq. 3 and SPEC convention.
      Some other packages (FullProf, CrysFML, hkl with ``tau=1``) use a
      no-2π variant where ``bᵢ · aⱼ = δᵢⱼ`` and ``|B @ h| = 1/d_hkl``.

      Cached until a lattice parameter changes.


   .. py:property:: a
      :type: float


      Lattice parameter a (Å).


   .. py:property:: alpha
      :type: float


      Lattice angle alpha (degrees).


   .. py:property:: b
      :type: float


      Lattice parameter b (Å).


   .. py:property:: beta
      :type: float


      Lattice angle beta (degrees).


   .. py:property:: c
      :type: float


      Lattice parameter c (Å).


   .. py:property:: cartesian_lattice_vectors
      :type: tuple[numpy.ndarray, numpy.ndarray, numpy.ndarray]


      Direct lattice vectors (a1, a2, a3) in Angstroms.

      a1 is along xHat, a2 lies in the xHat-yHat plane, a3 is
      determined by the right-hand rule.  Cached until a parameter changes.


   .. py:method:: from_dict(d: dict) -> Lattice
      :classmethod:


      Reconstruct a ``Lattice`` from a dict produced by :meth:`to_dict`.

      :param d: Must contain keys ``"a"``, ``"b"``, ``"c"``, ``"alpha"``,
                ``"beta"``, ``"gamma"``.
      :type d: dict

      :rtype: Lattice

      .. rubric:: Examples

      >>> Lattice.from_dict({'a': 5.431, 'b': 5.431, 'c': 5.431,
      ...                    'alpha': 90.0, 'beta': 90.0, 'gamma': 90.0})
      Lattice(system='cubic', a=5.431, b=5.431, c=5.431, ...)



   .. py:property:: gamma
      :type: float


      Lattice angle gamma (degrees).


   .. py:attribute:: precision
      :type:  int | None
      :value: None



   .. py:property:: reciprocal_lattice_vectors
      :type: tuple[numpy.ndarray, numpy.ndarray, numpy.ndarray]


      Reciprocal lattice vectors (b1, b2, b3) in Å⁻¹ (with 2π factor).

      Satisfies b_i · a_j = 2π δ_ij.  Cached until a parameter changes.


   .. py:property:: system
      :type: str


      Deduced crystal system name.


   .. py:method:: to_dict() -> dict

      Return a JSON-serialisable ``dict`` representing this lattice.

      All six parameters are always stored (even constrained ones), so
      the dict is unambiguous regardless of crystal system.

      :returns: Keys: ``"a"``, ``"b"``, ``"c"``, ``"alpha"``, ``"beta"``,
                ``"gamma"`` (all ``float``).
      :rtype: dict

      .. rubric:: Examples

      >>> Lattice(a=4.785, c=12.991, gamma=120).to_dict()
      {'a': 4.785, 'b': 4.785, 'c': 12.991, 'alpha': 90.0, 'beta': 90.0, 'gamma': 120.0}



.. py:class:: ModeDict(modes: dict[str, DiffractionMode] | None = None)

   An ordered, guarded dict of named diffraction modes.

   Only :class:`DiffractionMode` instances may be stored.  Keys are
   mode names (str).  Iteration follows insertion order.

   :param modes: Initial modes.  If ``None``, an empty dict is created.
   :type modes: dict[str, DiffractionMode] or None

   :raises TypeError: If any value is not a :class:`DiffractionMode` instance.

   .. rubric:: Examples

   >>> md = ModeDict({"bisecting": BisectingMode("omega", "ttheta")})
   >>> "bisecting" in md
   True
   >>> len(md)
   1


   .. py:method:: items()

      Return (name, mode) pairs.



   .. py:method:: keys()

      Return mode names.



   .. py:method:: values()

      Return mode objects.



.. py:function:: NEAREST_ANGLES(candidate: dict[str, float], previous: dict[str, float] | None) -> float

   Score a candidate motor-angle solution by its distance from the previous point.

   Returns the sum of squared angular differences between ``candidate`` and
   ``previous`` over all stages present in ``candidate``.  Lower score means
   closer to the previous point.

   When ``previous`` is ``None`` (first trajectory point) every candidate
   scores 0.0, so the first solution returned by ``forward()`` is used.

   :param candidate: Motor angles for one solution candidate.
   :type candidate: dict[str, float]
   :param previous: Motor angles chosen at the immediately preceding trajectory point.
   :type previous: dict[str, float] or None

   :returns: Non-negative score; lower is preferred.
   :rtype: float


.. py:function:: Q_mag_to_two_theta(Q_mag: float, wavelength: float) -> float

   Convert |Q| to 2θ.

   2θ = 2 arcsin(|Q| λ / (4π))

   :param Q_mag: Magnitude of the scattering vector in Å⁻¹.
   :type Q_mag: float
   :param wavelength: Wavelength in Å.
   :type wavelength: float

   :returns: 2θ in degrees.
   :rtype: float

   :raises ValueError: If ``Q_mag`` < 0, ``wavelength`` ≤ 0, or the reflection is
       unreachable (|Q| λ / (4π) > 1).

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> round(ahd.Q_mag_to_two_theta(2.0904, 1.5406), 2)
   30.0


.. py:function:: Q_to_d(Qx: float, Qy: float, Qz: float) -> float

   Convert a Q-vector to the d-spacing.

   d = 2π / |**Q**|

   :param Qx: Scattering vector components in Å⁻¹.
   :type Qx: float
   :param Qy: Scattering vector components in Å⁻¹.
   :type Qy: float
   :param Qz: Scattering vector components in Å⁻¹.
   :type Qz: float

   :returns: d-spacing in Å.
   :rtype: float

   :raises ValueError: If |Q| = 0.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> round(ahd.Q_to_d(1.0, 0.0, 0.0), 6)
   6.283185


.. py:function:: Q_to_hkl(sample: ad_hoc_diffractometer.sample.Sample, Qx: float, Qy: float, Qz: float) -> tuple[float, float, float]

   Convert a Q-vector (Å⁻¹) to Miller indices.

   Solves **hkl** = UB⁻¹ · **Q**.

   :param sample: Sample with a UB matrix set.
   :type sample: Sample
   :param Qx: Scattering vector components in Å⁻¹.
   :type Qx: float
   :param Qy: Scattering vector components in Å⁻¹.
   :type Qy: float
   :param Qz: Scattering vector components in Å⁻¹.
   :type Qz: float

   :returns: Miller indices (h, k, l).
   :rtype: tuple of float

   :raises ValueError: If ``sample.UB`` is None or singular.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> import numpy as np
   >>> g = ahd.fourcv()
   >>> g.sample.lattice = ahd.Lattice(a=3.905)
   >>> ahd.ub_identity(g.sample)
   >>> Q = ahd.hkl_to_Q(g.sample, 1, 0, 0)
   >>> ahd.Q_to_hkl(g.sample, *Q)  # doctest: +ELLIPSIS
   (1.0, 0.0, 0.0)


.. py:class:: Reflection

   One observed reflection used for diffractometer orientation.

   :param name: User-supplied label (e.g. ``"r1"``, ``"Si_111"``).  Set by
                ``ReflectionList.add()``.
   :type name: str
   :param hkl: Miller indices (h, k, l).  Need not be integers.
   :type hkl: tuple of float
   :param angles: Motor angles (degrees) keyed by stage name.  Keys are specific
                  to the geometry named in ``geometry_name`` and are validated by
                  ``ReflectionList.add()`` before the reflection is stored.
   :type angles: dict[str, float]
   :param wavelength: Wavelength in Å.  If None the geometry's wavelength is assumed.
   :type wavelength: float or None
   :param geometry_name: Name of the geometry on which this reflection was recorded.
                         Angle keys are only meaningful for that geometry.
   :type geometry_name: str or None

   .. rubric:: Notes

   Reflections are geometry-specific: angle keys from a ``psic`` geometry
   cannot be applied to ``kappa6c`` because the stage names differ.
   ``ReflectionList.add()`` validates keys and records ``geometry_name``
   so cross-geometry misuse can be detected.


   .. py:attribute:: angles
      :type:  dict[str, float]


   .. py:method:: from_dict(d: dict) -> Reflection
      :classmethod:


      Reconstruct a :class:`Reflection` from a dict produced by
      :meth:`to_dict`.

      :param d: Must contain ``"name"``, ``"hkl"``, ``"angles"``.
                ``"wavelength"`` and ``"geometry_name"`` are optional (default
                ``None``).
      :type d: dict

      :rtype: Reflection



   .. py:attribute:: geometry_name
      :type:  str | None
      :value: None



   .. py:attribute:: hkl
      :type:  tuple[float, float, float]


   .. py:attribute:: name
      :type:  str


   .. py:method:: to_dict() -> dict

      Return a JSON-serialisable ``dict`` representing this reflection.

      :returns: Keys: ``"name"`` (str), ``"hkl"`` (list of 3 float),
                ``"angles"`` (dict str→float), ``"wavelength"`` (float or None),
                ``"geometry_name"`` (str or None).
      :rtype: dict



   .. py:attribute:: wavelength
      :type:  float | None
      :value: None



.. py:class:: ReflectionList(geometry_name: str, valid_stages: set[str])

   Ordered, named collection of Reflection objects with or1/or2 management.

   Held as ``AdHocDiffractometer.reflections``.  All mutation methods
   validate that angle keys belong to the owning geometry.

   :param geometry_name: Name of the owning ``AdHocDiffractometer``.  Stored on every
                         reflection added through this list.
   :type geometry_name: str
   :param valid_stages: Stage names of the owning geometry.  Used to validate angle keys.
   :type valid_stages: set of str

   .. attribute:: geometry_name



      :type: str

   .. attribute:: valid_stages



      :type: set of str

   .. rubric:: Examples

   >>> rl = ReflectionList(geometry_name="psic",
   ...                     valid_stages={"mu", "eta", "chi", "phi",
   ...                                   "nu", "delta"})
   >>> r = rl.add("r1", hkl=(1, 0, 0), angles={"mu": 0, "eta": 20})
   >>> rl.setor1("r1")
   >>> rl.orienting_reflections
   [Reflection(name='r1', ...)]


   .. py:method:: add(name: str, hkl: tuple[float, float, float], angles: dict[str, float], wavelength: float | None = None) -> Reflection

      Add a named reflection, validate angle keys, and return it.

      :param name: Unique label.
      :type name: str
      :param hkl: Miller indices.
      :type hkl: tuple of float
      :param angles: Motor angles; keys must be stage names of this geometry.
      :type angles: dict[str, float]
      :param wavelength: Wavelength in Å; stored as-is (caller supplies the geometry's
                         current wavelength if desired).
      :type wavelength: float or None

      :raises ValueError: If ``name`` already exists or any angle key is not a valid stage.



   .. py:method:: clear() -> None

      Remove all reflections and clear or1/or2 designations.



   .. py:method:: from_dict(d: dict) -> ReflectionList
      :classmethod:


      Reconstruct a :class:`ReflectionList` from a dict produced by
      :meth:`to_dict`.

      :param d: Must contain ``"reflections"`` (list of reflection dicts).
                ``"geometry_name"``, ``"or1"``, and ``"or2"`` are optional.
      :type d: dict

      :rtype: ReflectionList



   .. py:attribute:: geometry_name


   .. py:method:: items()


   .. py:method:: keys()


   .. py:property:: orienting_reflections
      :type: list[Reflection]


      Ordered list of designated orienting reflections.

      Returns ``[]``, ``[or1]``, or ``[or1, or2]``.  A secondary
      without a primary is returned as a single-element list containing
      the secondary.


   .. py:method:: remove(name: str) -> None

      Remove the named reflection and clear any or1/or2 designation it held.

      :raises KeyError: If no reflection with that name exists.



   .. py:method:: setor1(reflection: str | Reflection) -> None

      Designate a reflection as primary (or1).

      If it was previously the secondary (or2), that designation is cleared.
      The previous or1 remains in the list without any designation.



   .. py:method:: setor2(reflection: str | Reflection) -> None

      Designate a reflection as secondary (or2).

      If it was previously the primary (or1), that designation is cleared.
      The previous or2 remains in the list without any designation.



   .. py:method:: to_dict() -> dict

      Return a JSON-serialisable ``dict`` representing this reflection list.

      :returns: Keys: ``"geometry_name"`` (str or None), ``"reflections"`` (list
                of reflection dicts in insertion order), ``"or1"`` (name str or
                None), ``"or2"`` (name str or None).
      :rtype: dict



   .. py:attribute:: valid_stages


   .. py:method:: values()


.. py:class:: Sample(name: str, lattice: ad_hoc_diffractometer.lattice.Lattice, reflections: ad_hoc_diffractometer.reflection.ReflectionList, U: numpy.ndarray | None = None, UB: numpy.ndarray | None = None, parent=None)

   One crystal sample mounted on a diffractometer.

   :param name: Unique label, matching the key in ``AdHocDiffractometer.samples``.
   :type name: str
   :param lattice: Crystal lattice; provides the B matrix.
   :type lattice: Lattice
   :param reflections: Geometry-specific reflection collection.  Must be constructed
                       with the owning geometry's stage names as ``valid_stages``.
   :type reflections: ReflectionList
   :param U: 3×3 crystal orientation matrix.  None until computed.
   :type U: numpy.ndarray or None
   :param UB: 3×3 UB = U @ B matrix.  None until computed.
   :type UB: numpy.ndarray or None

   .. rubric:: Notes

   U and UB are set by the U/UB computation routines (Priority 2 items
   #5 and #6).  They are stored here so that each sample carries its
   own orientation independently.


   .. py:attribute:: U
      :value: None



   .. py:attribute:: UB
      :value: None



   .. py:method:: from_dict(d: dict, parent=None) -> Sample
      :classmethod:


      Reconstruct a :class:`Sample` from a dict produced by :meth:`to_dict`.

      :param d: Must contain ``"name"``, ``"lattice"``, ``"reflections"``.
                ``"U"`` and ``"UB"`` are optional (default ``None``).
      :type d: dict
      :param parent: Geometry to attach as the parent back-reference.
      :type parent: AdHocDiffractometer or None

      :rtype: Sample



   .. py:attribute:: lattice


   .. py:attribute:: name


   .. py:attribute:: parent
      :value: None



   .. py:attribute:: reflections


   .. py:method:: to_dict() -> dict

      Return a JSON-serialisable ``dict`` representing this sample.

      :returns: Keys: ``"name"`` (str), ``"lattice"`` (lattice dict),
                ``"reflections"`` (reflection-list dict), ``"U"`` (3×3 list or
                None), ``"UB"`` (3×3 list or None).
      :rtype: dict



.. py:class:: SampleDict(active_ref: list[str])

   Guarded ordered dict of Sample objects.

   Enforces two invariants:
     1. Every value must be a Sample instance — no None, no arbitrary objects.
     2. The currently active sample cannot be removed or replaced.

   The active sample name is tracked via a reference to a one-element list
   ``[active_name]`` shared with the owning AdHocDiffractometer, so changes
   to the active selection are always visible here.

   Direct reassignment of the underlying container
   (``g._samples = something``) is blocked by making ``_samples`` a
   read-only property on AdHocDiffractometer.


   .. py:method:: clear() -> None


   .. py:method:: items()


   .. py:method:: keys()


   .. py:method:: pop(name: str, *args) -> Sample


   .. py:method:: values()


.. py:class:: Stage(name: str, axis: numpy.ndarray, parent: str | None = None, role: str = 'sample', angle: float = 0.0, limits: tuple[float, float] = (-180.0, 180.0))

   One rotary stage of a diffractometer.

   Each stage is characterised by:
     - a name (e.g. 'mu', 'eta', 'chi')
     - a rotation axis expressed as a signed Cartesian vector in the lab
       frame (e.g. +XHAT for right-handed vertical, -ZHAT for left-handed
       lateral).  Use axes.parse_axis() to construct this from the
       caller-facing notation.
     - a parent stage name (the stage on which this one sits), or None if
       it sits on the floor / fixed lab frame.

   The sign of the axis vector encodes handedness:
       +nHat  =>  right-handed rotation about nHat
       -nHat  =>  left-handed rotation about nHat

   :param name: Human-readable name for this stage (e.g. 'mu', 'S2-1').
   :type name: str
   :param axis: Signed rotation axis vector in the lab frame (internal representation).
                Construct from caller-facing notation using axes.parse_axis().
   :type axis: numpy.ndarray, shape (3,)
   :param parent: Name of the stage on which this stage is mounted, or None if it
                  sits directly on the lab frame (floor).
   :type parent: str or None
   :param role: 'sample' or 'detector', for bookkeeping.  Default is 'sample'.
   :type role: str, optional
   :param angle: Current angle setting in degrees.  Default is 0.0.
   :type angle: float, optional
   :param limits: (min_angle, max_angle) in degrees.  Default is (-180.0, 180.0).
                  Must satisfy min_angle < max_angle.
   :type limits: tuple of (float, float), optional

   :raises ValueError: If limits[0] >= limits[1].


   .. py:attribute:: angle
      :value: 0.0



   .. py:attribute:: axis


   .. py:method:: from_dict(d: dict) -> Stage
      :classmethod:


      Reconstruct a :class:`Stage` from a dict produced by :meth:`to_dict`.

      :param d: Must contain ``"name"``, ``"axis"``, ``"role"``.  ``"parent"``,
                ``"angle"``, and ``"limits"`` are optional (defaulting to
                ``None``, ``0.0``, and ``(-180.0, 180.0)`` respectively).
      :type d: dict

      :rtype: Stage



   .. py:method:: in_limits(angle_deg: float) -> bool

      Return True if angle_deg is within [min_angle, max_angle] (inclusive).

      :param angle_deg: Angle to test, in degrees.
      :type angle_deg: float

      :rtype: bool



   .. py:property:: limits
      :type: tuple[float, float]


      Motor angle limits (min_angle, max_angle) in degrees.


   .. py:attribute:: name


   .. py:attribute:: parent
      :value: None



   .. py:attribute:: role
      :value: 'sample'



   .. py:method:: rotation_matrix() -> numpy.ndarray

      Return the 3x3 rotation matrix for the current angle setting.



   .. py:method:: to_dict() -> dict

      Return a JSON-serialisable ``dict`` representing this stage.

      :returns: Keys: ``"name"`` (str), ``"axis"`` (list of 3 float),
                ``"role"`` (str), ``"parent"`` (str or None),
                ``"angle"`` (float), ``"limits"`` (list of 2 float).
      :rtype: dict



.. py:data:: XHAT

.. py:data:: XRAY_LINES
   :type:  dict[str, float]

.. py:data:: YHAT

.. py:data:: ZHAT

.. py:function:: allclose(a, b, atol: float | None = None, digits: int | None = None) -> bool

   Tolerance-aware comparison for scalars or array-like sequences.

   Uses ``np.allclose`` with ``rtol=0`` and an absolute tolerance derived
   from the display precision (or an explicit value).

   :param a: Values to compare.
   :type a: scalar or array-like
   :param b: Values to compare.
   :type b: scalar or array-like
   :param atol: Explicit absolute tolerance.  If None, derived from ``digits``.
   :type atol: float or None
   :param digits: Display precision to derive tolerance from.  If None, uses
                  ``get_precision()``.  Ignored when ``atol`` is given explicitly.
   :type digits: int or None

   :rtype: bool

   .. rubric:: Examples

   >>> allclose(1.0000001, 1.0000002)          # within default 6-digit tol
   True
   >>> allclose(1.001, 1.002, digits=3)        # within 3-digit tol (0.0005)
   True
   >>> allclose(1.001, 1.002, digits=4)        # outside 4-digit tol (0.00005)
   False
   >>> allclose([1.0, 2.0], [1.0, 2.0])
   True


.. py:function:: alpha_f(geometry: ad_hoc_diffractometer.geometry.AdHocDiffractometer, angles: dict[str, float] | None = None) -> float

   Compute the angle of emergence αf (degrees).

   αf is the angle between the diffracted beam and the sample surface::

       sin(αf) = |k̂f · n̂_lab|

   :param geometry: Must have ``wavelength``, ``sample.UB``, and ``surface_normal``
                    (or ``azimuthal_reference``) set.
   :type geometry: AdHocDiffractometer
   :param angles: Motor angles in degrees.  If ``None``, current stage angles are used.
   :type angles: dict[str, float] or None

   :returns: αf in degrees, in [0°, 90°].
   :rtype: float

   :raises ValueError: If any required attribute is not set.

   .. rubric:: References

   Lohmeier & Vlieg (1993), §4.2, eq. 16.


.. py:function:: alpha_i(geometry: ad_hoc_diffractometer.geometry.AdHocDiffractometer, angles: dict[str, float] | None = None) -> float

   Compute the angle of incidence αᵢ (degrees).

   αᵢ is the angle between the incoming beam and the sample surface
   (complement of the angle between the beam and the surface normal)::

       sin(αᵢ) = |ŷ · n̂_lab|

   :param geometry: Must have ``wavelength``, ``sample.UB``, and ``surface_normal``
                    (or ``azimuthal_reference``) set.
   :type geometry: AdHocDiffractometer
   :param angles: Motor angles in degrees.  If ``None``, current stage angles are used.
   :type angles: dict[str, float] or None

   :returns: αᵢ in degrees, in [0°, 90°].
   :rtype: float

   :raises ValueError: If any required attribute is not set.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> g = ahd.zaxis()
   >>> g.wavelength = 1.5406
   >>> g.surface_normal = (0, 0, 1)
   >>> ahd.ub_identity(g.sample)
   >>> ai = g.alpha_i({"alpha": 5.0, "Z": 0.0, "delta": 20.0, "gamma": 0.0})
   >>> round(ai, 4)
   5.0

   .. rubric:: References

   Lohmeier & Vlieg (1993), §4.2, eq. 15.


.. py:function:: angles_to_phi_vector(geometry, **motor_angles: float) -> numpy.ndarray

   Convert a set of motor angles to the scattering vector in the phi frame.

   The "phi frame" is the coordinate system seen from the innermost sample
   stage — the frame in which crystal reflections are expressed when
   computing the orientation (U) matrix.

   Algorithm (Busing & Levy 1967, section "The phi-axis frame"):

   1. Temporarily set the supplied motor angles on their stages (preserving
      the original values so the geometry is restored afterwards).
   2. Compute the total sample rotation matrix ``Z`` (product of all sample
      stage rotation matrices, floor-most first).
   3. Compute the total detector rotation matrix ``D``.
   4. The incident-beam unit vector in the lab frame is ``ŷ`` (longitudinal
      direction, ``geometry.basis["longitudinal"]``).
   5. The scattered-beam unit vector in the lab frame is ``D @ ŷ``.
   6. The scattering vector in the lab frame is::

          Q_lab = (2π / λ) * (D @ ŷ - ŷ)

   7. Rotate Q_lab back through the sample stack::

          Q_phi = Z⁻¹ @ Q_lab = Zᵀ @ Q_lab

      (Z is orthogonal, so Z⁻¹ = Zᵀ.)

   :param geometry: The diffractometer geometry.  Must have ``wavelength`` set (not None).
   :type geometry: AdHocDiffractometer
   :param \*\*motor_angles: Motor angles in degrees, keyed by stage name.  All stages present in
                            the geometry may be supplied; stages not supplied keep their current
                            ``angle`` attribute.  Only sample and detector stages affect the
                            result; other stages (if any) are ignored.
   :type \*\*motor_angles: float

   :returns: **Q_phi** -- Scattering vector in the phi frame, in units of Å⁻¹.
   :rtype: numpy.ndarray, shape (3,)

   :raises KeyError: If a supplied stage name does not exist in the geometry.
   :raises ValueError: If ``geometry.wavelength`` is None.
   :raises ValueError: If the geometry has no sample stages.
   :raises ValueError: If the geometry has no detector stages.

   .. rubric:: Notes

   The function modifies stage angles temporarily and restores them
   afterwards, even if an exception is raised.  It is therefore safe to
   call inside a ``try`` block or from multiple threads as long as each
   call uses a separate geometry instance.

   The scattering vector Q_phi is independent of which sample stage is
   designated the "phi" axis; it is expressed in the frame of the *last*
   sample stage in the stacking order (the one closest to the sample).

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> g = ahd.psic()
   >>> g.wavelength = 1.5406          # Cu Kα in Å
   >>> Q_phi = ahd.angles_to_phi_vector(
   ...     g,
   ...     mu=0, eta=20.97, chi=90, phi=0, nu=0, delta=41.94,
   ... )
   >>> Q_phi  # scattering vector for sapphire (006) in phi frame
   array([...])

   .. rubric:: References

   Busing & Levy, Acta Cryst. 22, 457-464 (1967) — phi-axis frame
   You, J. Appl. Cryst. 32, 614-623 (1999) — psic geometry conventions


.. py:function:: axis_from_physical(direction: str, sign: str, basis: dict) -> numpy.ndarray

   Convenience wrapper: resolve a physical direction and sign to an axis vector.

   :param direction: Physical direction name, e.g. "vertical", "lateral", "longitudinal".
   :type direction: str
   :param sign: "+" for right-handed, "-" for left-handed rotation.
   :type sign: str
   :param basis: Mapping from physical direction names to numpy arrays.
   :type basis: dict

   :returns: **axis**
   :rtype: numpy.ndarray, shape (3,)

   .. rubric:: Examples

   >>> basis = {"vertical": XHAT, "longitudinal": YHAT, "lateral": ZHAT}
   >>> axis_from_physical("lateral", "-", basis)
   array([ 0.,  0., -1.])


.. py:function:: axis_label(vector: numpy.ndarray) -> str

   Convert an internal axis numpy array to its caller-facing string label.

   Only the six standard signed basis vectors are recognised.  For other
   vectors (e.g. a kappa-style tilted axis) the array is formatted
   numerically.

   :param vector: Signed axis vector.
   :type vector: numpy.ndarray, shape (3,)

   :returns: **label** -- Caller-facing label, e.g. "+x", "-z", or "[0.5, 0.5, 0.707]".
   :rtype: str

   .. rubric:: Examples

   >>> axis_label(XHAT)
   '+x'
   >>> axis_label(-ZHAT)
   '-z'


.. py:function:: b_matrix(b1: numpy.ndarray, b2: numpy.ndarray, b3: numpy.ndarray) -> numpy.ndarray

   Compute the B matrix from the reciprocal lattice vectors.

   The B matrix transforms Miller indices **h** = (h, k, l) to the
   scattering vector in Cartesian crystal-frame coordinates
   (Busing & Levy, 1967, eq. 3):

       Q_c = B @ h

   The columns of B are the reciprocal lattice vectors **b**₁, **b**₂,
   **b**₃ (each including the 2π factor), so:

       B.T = [b1, b2, b3]    (column-stack of the reciprocal vectors)

   The orthogonality condition is ``bᵢ · aⱼ = 2π δᵢⱼ``, and
   ``|B @ h| = 2π / d_hkl``.

   This is the Busing & Levy (1967) and SPEC convention.  Some packages
   (FullProf, CrysFML, parts of CCP4, the hkl library with ``tau=1``)
   use the alternative no-2π convention ``bᵢ · aⱼ = δᵢⱼ``, which gives
   a B matrix smaller by a factor of 2π.

   :param b1: Reciprocal lattice vectors in inverse Angstroms (with 2π factor),
              as returned by :func:`reciprocal_vectors`.
   :type b1: numpy.ndarray, shape (3,)
   :param b2: Reciprocal lattice vectors in inverse Angstroms (with 2π factor),
              as returned by :func:`reciprocal_vectors`.
   :type b2: numpy.ndarray, shape (3,)
   :param b3: Reciprocal lattice vectors in inverse Angstroms (with 2π factor),
              as returned by :func:`reciprocal_vectors`.
   :type b3: numpy.ndarray, shape (3,)

   :returns: **B** -- B matrix in inverse Angstroms (with 2π factor, BL1967 convention).
   :rtype: numpy.ndarray, shape (3, 3)


.. py:function:: compute_forward(geometry: ad_hoc_diffractometer.geometry.AdHocDiffractometer, h: float, k: float, l: float) -> list[dict[str, float]]

   Compute all valid motor-angle solutions for the reciprocal-lattice point
   (h, k, l) in the geometry's active diffraction mode.

   :param geometry: The diffractometer.  Must have ``wavelength`` and ``sample.UB`` set,
                    and an active ``mode_name``.
   :type geometry: AdHocDiffractometer
   :param h: Miller indices of the target reflection.
   :type h: float
   :param k: Miller indices of the target reflection.
   :type k: float
   :param l: Miller indices of the target reflection.
   :type l: float

   :returns: Each element is a complete set of motor angles (all stage names as
             keys, values in degrees) that satisfies the Bragg condition under
             the active mode constraints.  May contain zero, one, or multiple
             solutions depending on the geometry and mode.
   :rtype: list of dict[str, float]

   :raises ValueError: If ``geometry.wavelength`` is None.
   :raises ValueError: If ``geometry.sample.UB`` is None.
   :raises ValueError: If (h, k, l) == (0, 0, 0).
   :raises ValueError: If the scattering vector magnitude exceeds the Ewald sphere
       (|Q| > 4π/λ, i.e. Bragg condition cannot be satisfied).
   :raises NotImplementedError: If no active mode is set or the active mode type is not supported
       for this geometry.


.. py:function:: d_to_Q_mag(d: float) -> float

   Convert a d-spacing to the magnitude of the scattering vector.

   |**Q**| = 2π / d

   :param d: d-spacing in Å.
   :type d: float

   :returns: |Q| in Å⁻¹.
   :rtype: float

   :raises ValueError: If ``d`` ≤ 0.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> round(ahd.d_to_Q_mag(1.0), 6)
   6.283185


.. py:function:: d_to_two_theta(d: float, wavelength: float) -> float

   Convert d-spacing to 2θ using Bragg's law.

   λ = 2 d sin(θ)  →  2θ = 2 arcsin(λ / (2d))

   :param d: d-spacing in Å.
   :type d: float
   :param wavelength: Wavelength in Å.
   :type wavelength: float

   :returns: 2θ in degrees.
   :rtype: float

   :raises ValueError: If ``d`` ≤ 0, ``wavelength`` ≤ 0, or λ / (2d) > 1 (reflection
       unreachable at this wavelength).

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> round(ahd.d_to_two_theta(2.0, 1.5406), 4)
   22.6956


.. py:function:: energy_to_wavelength(energy_kev: float) -> float

   Convert photon energy to wavelength for X-rays.

   λ (Å) = hc / E = 12.39842 / E (keV)

   :param energy_kev: Photon energy in keV.
   :type energy_kev: float

   :returns: Wavelength in Å.
   :rtype: float

   :raises ValueError: If ``energy_kev`` ≤ 0.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> round(ahd.energy_to_wavelength(8.047), 4)
   1.5408


.. py:function:: fivec(basis: dict = _BASIS_YOU) -> ad_hoc_diffractometer.geometry.AdHocDiffractometer

   Five-circle diffractometer (fourcv mounted on a vertical base).

   Walko (2016) designation: (S3D1)1.
   Default basis: You (1999) — vertical=+x, longitudinal=+y, lateral=+z.

   A standard Eulerian four-circle (fourcv) is mounted on a fifth vertical
   rotation stage (mu) as a base.  The sample and detector motions are coupled
   through mu.  This provides an additional degree of freedom for accessing
   wider regions of reciprocal space, particularly at synchrotron sources.

   Stack (floor first):
       mu (shared base): vertical,     right-handed
         --> omega (sample): lateral,      left-handed
               --> chi:      longitudinal, right-handed
                     --> phi: lateral,     left-handed
         --> ttheta (detector): lateral,   left-handed

   References: E. Vlieg et al., J. Appl. Cryst. 20, 330-337 (1987).
               D.A. Walko, Ref. Module Mater. Sci. Mater. Eng. (2016).


.. py:function:: fmt(value: float, digits: int | None = None) -> str

   Format a float for display with the given number of decimal places.

   :param value: The value to format.
   :type value: float
   :param digits: Number of decimal places.  If None, uses the package-level default
                  from get_precision().
   :type digits: int or None

   :returns: Formatted string, e.g. '4.785000' for fmt(4.785, 6).
   :rtype: str


.. py:function:: fourch(basis: dict = _BASIS_BL) -> ad_hoc_diffractometer.geometry.AdHocDiffractometer

   Four-circle Eulerian diffractometer, horizontal scattering plane (laboratory).

   Walko (2016) designation: S3D1.

   Laboratory / default configuration: omega and ttheta both rotate about
   the vertical axis, so the scattering plane is horizontal.  This is the
   geometry described in Busing & Levy (1967), Fig. 1b.

   Default basis: Busing & Levy (1967) — lateral=+x, longitudinal=+y, vertical=+z.

   Sample stack (floor first):
       omega  : vertical, left-handed
       chi    : lateral,  right-handed
       phi    : vertical, left-handed

   Detector (floor, mechanically independent):
       ttheta : vertical, left-handed

   omega and ttheta share the same vertical axis; mechanically independent.

   Reference: W.R. Busing & H.A. Levy, Acta Cryst. 22, 457-464 (1967).
              D.A. Walko, Ref. Module Mater. Sci. Mater. Eng. (2016).


.. py:function:: fourcv(basis: dict = _BASIS_BL) -> ad_hoc_diffractometer.geometry.AdHocDiffractometer

   Four-circle Eulerian diffractometer, vertical scattering plane (synchrotron).

   Walko (2016) designation: S3D1.

   Synchrotron configuration: omega and ttheta both rotate about the
   lateral axis, so the scattering plane is vertical.  This exploits the
   s-polarisation and tighter vertical collimation of synchrotron radiation
   (Walko 2016).

   Default basis: Busing & Levy (1967) — lateral=+x, longitudinal=+y, vertical=+z.

   Sample stack (floor first):
       omega  : lateral,      left-handed
       chi    : longitudinal, right-handed
       phi    : lateral,      left-handed

   Detector (floor, mechanically independent of sample stack):
       ttheta : lateral,      left-handed

   omega and ttheta share the same lateral axis; mechanically independent.

   References: W.R. Busing & H.A. Levy, Acta Cryst. 22, 457-464 (1967).
               D.A. Walko, Ref. Module Mater. Sci. Mater. Eng. (2016).


.. py:function:: get_geometry(name: str)

   Return the registered factory function for the named geometry.

   This is the primitive lookup — it returns the callable factory, not an
   instance.  Use make_geometry() if you want an AdHocDiffractometer
   instance directly.

   :param name: Name of the geometry, as registered by @register_geometry
                (e.g. 'psic', 'fourcv', 'kappa4cv').
   :type name: str

   :returns: The factory function for the named geometry.
   :rtype: callable

   :raises ValueError: If no geometry with that name is registered, with a message listing
       the available names.

   .. rubric:: Examples

   >>> from ad_hoc_diffractometer import get_geometry
   >>> factory = get_geometry("psic")
   >>> factory()
   AdHocDiffractometer(name='psic', ...)
   >>> get_geometry("kappa4cv")(alpha_deg=50)
   AdHocDiffractometer(name='kappa4cv', ...)


.. py:function:: get_precision() -> int

   Return the current package-level display precision (decimal places).


.. py:function:: hkl_to_Q(sample: ad_hoc_diffractometer.sample.Sample, h: float, k: float, l: float) -> numpy.ndarray

   Convert Miller indices to the scattering vector Q in Å⁻¹.

   Uses the UB matrix: **Q** = UB · **hkl**.

   :param sample: Sample with a UB matrix set.
   :type sample: Sample
   :param h: Miller indices.
   :type h: float
   :param k: Miller indices.
   :type k: float
   :param l: Miller indices.
   :type l: float

   :returns: Scattering vector (Qx, Qy, Qz) in Å⁻¹.
   :rtype: numpy.ndarray, shape (3,)

   :raises ValueError: If ``sample.UB`` is None.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> g = ahd.fourcv()
   >>> g.sample.lattice = ahd.Lattice(a=3.905)
   >>> ahd.ub_identity(g.sample)
   >>> Q = ahd.hkl_to_Q(g.sample, 1, 0, 0)
   >>> float(Q[0])  # doctest: +ELLIPSIS
   1.6...


.. py:function:: hkl_to_d(sample: ad_hoc_diffractometer.sample.Sample, h: float, k: float, l: float) -> float

   Convert Miller indices to d-spacing.

   d = 2π / |UB · **hkl**|

   :param sample: Sample with a UB matrix set.
   :type sample: Sample
   :param h: Miller indices.
   :type h: float
   :param k: Miller indices.
   :type k: float
   :param l: Miller indices.
   :type l: float

   :returns: d-spacing in Å.
   :rtype: float

   :raises ValueError: If ``sample.UB`` is None or if **hkl** = (0, 0, 0).

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> g = ahd.fourcv()
   >>> g.sample.lattice = ahd.Lattice(a=3.905)
   >>> ahd.ub_identity(g.sample)
   >>> round(ahd.hkl_to_d(g.sample, 1, 0, 0), 4)
   3.905


.. py:function:: hkl_to_two_theta(sample: ad_hoc_diffractometer.sample.Sample, h: float, k: float, l: float, wavelength: float) -> float

   Convert Miller indices to 2θ using the UB matrix and Bragg's law.

   :param sample: Sample with a UB matrix set.
   :type sample: Sample
   :param h: Miller indices.
   :type h: float
   :param k: Miller indices.
   :type k: float
   :param l: Miller indices.
   :type l: float
   :param wavelength: Wavelength in Å.
   :type wavelength: float

   :returns: 2θ in degrees.
   :rtype: float

   :raises ValueError: If ``sample.UB`` is None, ``wavelength`` ≤ 0, **hkl** = (0, 0, 0),
       or the reflection is unreachable at this wavelength.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> g = ahd.fourcv()
   >>> g.sample.lattice = ahd.Lattice(a=3.905)
   >>> ahd.ub_identity(g.sample)
   >>> round(ahd.hkl_to_two_theta(g.sample, 1, 0, 0, 1.5406), 4)
   23.2066


.. py:function:: hkl_trajectory(geometry: ad_hoc_diffractometer.geometry.AdHocDiffractometer, trajectory: dict, n_points: int, *, solution_key=NEAREST_ANGLES) -> list[dict]

   Compute motor-angle sequences along a reciprocal-space trajectory.

   For each of the ``n_points`` equally-spaced hkl positions along the
   trajectory, calls ``geometry.forward()`` to find all valid motor-angle
   solutions, then uses ``solution_key`` to select one.  Points with no
   valid solution are included with ``angles=None``.

   The ``NEAREST_ANGLES`` solution key (the default) prevents branch-flip
   discontinuities: at each point it selects the candidate whose motor
   angles are closest to those of the previous point.

   :param geometry: Must have ``wavelength`` and ``sample.UB`` set and an active mode.
   :type geometry: AdHocDiffractometer
   :param trajectory: Trajectory specification.  The ``"type"`` key selects the scan
                      shape; see module docstring for supported types and required keys.
   :type trajectory: dict
   :param n_points: Number of evenly-spaced points to compute (≥ 2).
   :type n_points: int
   :param solution_key: Scoring function ``(candidate, previous) -> float``; lower wins.
                        Default: ``NEAREST_ANGLES``.  Pass ``None`` to use ``forward()``'s
                        raw ordering.
   :type solution_key: callable or None, optional

   :returns: One entry per trajectory point with keys:

             ``"hkl"`` : tuple of float
                 The requested Miller indices (h, k, l).
             ``"angles"`` : dict[str, float] or None
                 Motor angles in degrees, or ``None`` if no valid solution.
             ``"warning"`` : str or None
                 Descriptive warning when ``angles`` is ``None``, else ``None``.
   :rtype: list of dict

   :raises ValueError: If ``n_points < 2``, trajectory type is unknown, or a required
       trajectory key is missing.
   :raises ValueError: If ``geometry.wavelength`` is None or ``geometry.sample.UB`` is None.
   :raises NotImplementedError: If no active diffraction mode is set.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> g = ahd.fourcv()
   >>> g.wavelength = 1.5406
   >>> g.sample.lattice = ahd.Lattice(a=4.0)
   >>> ahd.ub_identity(g.sample)
   >>> g.mode_name = "bisecting"
   >>> result = ahd.hkl_trajectory(
   ...     g,
   ...     {"type": "line", "start": (1, 0, 0), "end": (3, 0, 0)},
   ...     n_points=5,
   ... )
   >>> for pt in result:
   ...     print(pt["hkl"], pt["angles"])


.. py:function:: is_evanescent(geometry: ad_hoc_diffractometer.geometry.AdHocDiffractometer, angles: dict[str, float] | None = None, critical_angle_deg: float | None = None) -> bool

   Return True when αᵢ is below the critical angle (evanescent / surface-sensitive).

   If ``critical_angle_deg`` is ``None`` the function raises ``ValueError``
   since the critical angle depends on material properties (electron density)
   that are not stored on the geometry.

   :param geometry:
   :type geometry: AdHocDiffractometer
   :param angles: Motor angles in degrees.
   :type angles: dict[str, float] or None
   :param critical_angle_deg: Critical angle for total external reflection in degrees.  This is
                              a material property (typically 0.1°–0.5° for hard X-rays) that
                              must be supplied by the caller.
   :type critical_angle_deg: float

   :returns: True if αᵢ < critical_angle_deg.
   :rtype: bool

   :raises ValueError: If ``critical_angle_deg`` is None.

   .. rubric:: Examples

   >>> g.is_evanescent(critical_angle_deg=0.2)
   True   # if αᵢ < 0.2°


.. py:function:: is_specular(geometry: ad_hoc_diffractometer.geometry.AdHocDiffractometer, angles: dict[str, float] | None = None, atol: float = 0.01) -> bool

   Return True when αᵢ ≈ αf within ``atol`` degrees.

   The specular condition (αᵢ = αf) is the constraint used in
   reflectometry and specular surface diffraction.

   :param geometry:
   :type geometry: AdHocDiffractometer
   :param angles: Motor angles in degrees.
   :type angles: dict[str, float] or None
   :param atol: Tolerance in degrees.  Default is 0.01°.
   :type atol: float

   :rtype: bool

   :raises ValueError: If any required attribute is not set.


.. py:function:: kappa4ch(alpha_deg: float = KAPPA_ALPHA_DEFAULT, basis: dict = _BASIS_BL) -> ad_hoc_diffractometer.geometry.AdHocDiffractometer

   Four-circle kappa diffractometer, horizontal scattering plane (laboratory).

   Walko (2016) designation: S3D1.

   Identical to kappa4cv() but komega and ttheta rotate about the vertical
   axis, giving a horizontal scattering plane (laboratory convention).

   Default basis: Busing & Levy (1967) — lateral=+x, longitudinal=+y, vertical=+z.

   Sample stack (floor first):
       komega : vertical, left-handed
       kappa  : tilted,   kappa_axis(alpha), right-handed
       kphi   : vertical, left-handed

   Detector (floor, mechanically independent):
       ttheta : vertical, left-handed

   komega and ttheta share the same vertical axis; mechanically independent.

   :param alpha_deg: Kappa tilt angle in degrees (default 50).  Must be in (0, 90).
   :type alpha_deg: float
   :param References: W.R. Busing & H.A. Levy, Acta Cryst. 22, 457-464 (1967).
                      ITC Vol. C, Sec. 2.2.6 (2006).
   :type References: D.A. Walko, Ref. Module Mater. Sci. Mater. Eng. (2016).


.. py:function:: kappa4cv(alpha_deg: float = KAPPA_ALPHA_DEFAULT, basis: dict = _BASIS_BL) -> ad_hoc_diffractometer.geometry.AdHocDiffractometer

   Four-circle kappa diffractometer, vertical scattering plane (synchrotron).

   Walko (2016) designation: S3D1.

   The chi circle of a standard Eulerian fourcv is replaced by a kappa arm.
   The kappa axis lies in the vertical-lateral plane, tilted alpha degrees
   from the vertical toward the lateral axis.

   komega and ttheta both rotate about the lateral axis; the scattering
   plane is vertical (synchrotron convention).

   Default basis: Busing & Levy (1967) — lateral=+x, longitudinal=+y, vertical=+z.

   Sample stack (floor first):
       komega : lateral, left-handed
       kappa  : tilted,  kappa_axis(alpha), right-handed
       kphi   : lateral, left-handed

   Detector (floor, mechanically independent):
       ttheta : lateral, left-handed

   komega and ttheta share the same lateral axis; mechanically independent.

   :param alpha_deg: Kappa tilt angle in degrees (default 50).  Must be in (0, 90).
   :type alpha_deg: float
   :param References: W.R. Busing & H.A. Levy, Acta Cryst. 22, 457-464 (1967).
                      ITC Vol. C, Sec. 2.2.6 (2006).
   :type References: D.A. Walko, Ref. Module Mater. Sci. Mater. Eng. (2016).


.. py:function:: kappa6c(alpha_deg: float = KAPPA_ALPHA_DEFAULT, basis: dict = _BASIS_YOU) -> ad_hoc_diffractometer.geometry.AdHocDiffractometer

   Six-circle kappa diffractometer (psic-style outer axes, kappa inner sample).

   Walko (2016) designation: S4D2.

   Extends the kappa4cv geometry with two additional axes (mu, nu) in
   the style of the psic geometry (You 1999), giving full orientation freedom.
   This is the synchrotron configuration with a lateral detector.

   Default basis: You (1999) — vertical=+x, longitudinal=+y, lateral=+z.

   Sample stack (floor first):
       mu     : vertical,     right-handed   [outermost]
       komega : lateral,      left-handed
       kappa  : tilted,       kappa_axis(alpha), right-handed
       kphi   : lateral,      left-handed

   Detector stack (floor first):
       nu     : vertical,     right-handed
       delta  : lateral,      left-handed

   mu and nu share the same vertical axis; mechanically independent.

   :param alpha_deg: Kappa tilt angle in degrees (default 50).  Must be in (0, 90).
   :type alpha_deg: float
   :param References:
                      H. You, J. Appl. Cryst. 32, 614-623 (1999).
                      ITC Vol. C, Sec. 2.2.6 (2006).
   :type References: D.A. Walko, Ref. Module Mater. Sci. Mater. Eng. (2016).


.. py:function:: kappa_axis(alpha_deg: float, basis: dict | None = None) -> numpy.ndarray

   Compute the kappa rotation axis vector for a kappa-geometry diffractometer.

   The kappa axis lies in the vertical-lateral plane, tilted at angle alpha
   from the vertical axis toward the lateral axis:


       kappa_axis = vertical * cos(alpha) + lateral * sin(alpha)

   In the You (1999) / default basis (xHat=vertical, zHat=lateral):

       kappa_axis = XHAT * cos(alpha) + ZHAT * sin(alpha)

   At alpha=0  the axis is purely vertical (degenerate with komega).
   At alpha=90 the axis is purely lateral.
   Typical value: alpha=50 degrees (Walko 2016; ITC Vol. C Sec. 2.2.6;
   originally Enraf-Nonius).

   The axis is a unit vector by construction (cos²α + sin²α = 1).

   :param alpha_deg: Kappa tilt angle in degrees, measured from the vertical axis toward
                     the lateral axis in the vertical-lateral plane.  Must be in (0, 90).
   :type alpha_deg: float
   :param basis: Mapping from physical direction names to numpy arrays.  If None,
                 the default You (1999) basis is used:
                     vertical  -> XHAT
                     lateral   -> ZHAT
   :type basis: dict or None

   :returns: **axis** -- Unit vector of the kappa rotation axis in the lab frame.
   :rtype: numpy.ndarray, shape (3,)

   :raises ValueError: If alpha_deg is not in the open interval (0, 90) degrees, or if
       the basis dict is missing 'vertical' or 'lateral' keys.

   .. rubric:: Examples

   >>> kappa_axis(50.0)                    # typical kappa, default basis
   >>> kappa_axis(50.0, basis=my_basis)    # custom basis


.. py:function:: lattice_vectors(a: float, b: float, c: float, alpha: float, beta: float, gamma: float) -> tuple[numpy.ndarray, numpy.ndarray, numpy.ndarray]

   Compute the three Cartesian direct lattice vectors from crystal lattice
   parameters.

   The convention places a1 along xHat, a2 in the xHat-yHat plane, and a3
   determined by the right-hand rule.

   :param a: Lattice parameters in Angstroms.
   :type a: float
   :param b: Lattice parameters in Angstroms.
   :type b: float
   :param c: Lattice parameters in Angstroms.
   :type c: float
   :param alpha: Lattice angles in degrees.
                 alpha = angle between b and c axes
                 beta  = angle between a and c axes
                 gamma = angle between a and b axes
   :type alpha: float
   :param beta: Lattice angles in degrees.
                alpha = angle between b and c axes
                beta  = angle between a and c axes
                gamma = angle between a and b axes
   :type beta: float
   :param gamma: Lattice angles in degrees.
                 alpha = angle between b and c axes
                 beta  = angle between a and c axes
                 gamma = angle between a and b axes
   :type gamma: float

   :returns: **a1, a2, a3** -- Cartesian direct lattice vectors in Angstroms.
   :rtype: numpy.ndarray, shape (3,)


.. py:function:: list_geometries() -> dict[str, type]

   Return a copy of the geometry registry as {name: factory_callable}.

   Includes all built-in geometries (registered via ``@register_geometry``
   at import time) plus any third-party geometry plugins installed as
   entry points in the ``"ad_hoc_diffractometer.geometries"`` group.

   Entry-point discovery runs automatically the first time this function
   is called; subsequent calls use the already-populated registry.

   :returns: Keys are factory names (e.g. ``'psic'``, ``'fourcv'``).
             Values are the callable factory functions.
   :rtype: dict

   .. rubric:: Examples

   >>> from ad_hoc_diffractometer import list_geometries
   >>> sorted(list_geometries())
   ['fivec', 'fourch', 'fourcv', 'kappa4ch', 'kappa4cv', 'kappa6c',
    'psic', 's2d2', 'sixc', 'zaxis']
   >>> list_geometries()['psic']()   # instantiate by name
   AdHocDiffractometer(name='psic', ...)


.. py:function:: make_geometry(name: str, **kwargs) -> ad_hoc_diffractometer.geometry.AdHocDiffractometer

   Instantiate a geometry by name, passing keyword arguments to its factory.

   Looks up the factory for the named geometry via get_geometry() and calls
   it with the supplied kwargs.  This is the most convenient entry point for
   config-driven or programmatic geometry selection.

   :param name: Name of the geometry (e.g. 'psic', 'fourcv', 'kappa4cv').
   :type name: str
   :param \*\*kwargs: Keyword arguments forwarded to the factory function.  Most factories
                      take no arguments; kappa factories accept alpha_deg.

   :returns: A fully configured diffractometer geometry instance.
   :rtype: AdHocDiffractometer

   :raises ValueError: If no geometry with that name is registered.

   .. rubric:: Examples

   >>> from ad_hoc_diffractometer import make_geometry
   >>> make_geometry("psic")
   AdHocDiffractometer(name='psic', ...)
   >>> make_geometry("kappa4cv", alpha_deg=50)
   AdHocDiffractometer(name='kappa4cv', ...)
   >>> make_geometry("kappa6c", alpha_deg=55)
   AdHocDiffractometer(name='kappa6c', ...)


.. py:function:: parse_axis(label: str, basis: dict | None = None) -> numpy.ndarray

   Convert a caller-facing axis label to an internal numpy array.

   Accepted forms:

   1. Signed Cartesian label:  "+x", "-x", "+y", "-y", "+z", "-z"
      These are resolved directly against the standard basis vectors
      XHAT, YHAT, ZHAT regardless of the supplied basis dict.

   2. Physical direction name with optional sign prefix:
      "vertical", "+vertical", "-vertical",
      "longitudinal", "+longitudinal", "-longitudinal",
      "lateral", "+lateral", "-lateral"
      These are resolved against the supplied basis dict.

   :param label: Axis label in one of the forms above.  Case-insensitive.
   :type label: str
   :param basis: Mapping from physical direction names to numpy arrays.
                 Required if label contains a physical direction name.
                 Ignored if label is a signed Cartesian label.
   :type basis: dict or None

   :returns: **axis** -- Signed axis vector for internal use.
   :rtype: numpy.ndarray, shape (3,)

   :raises ValueError: If the label is not recognised, or if a physical direction name is
       given but no basis dict is supplied, or if the direction name is
       not in the basis dict.

   .. rubric:: Examples

   >>> parse_axis("+x")
   array([1., 0., 0.])
   >>> parse_axis("-z")
   array([ 0.,  0., -1.])
   >>> basis = {"vertical": XHAT, "longitudinal": YHAT, "lateral": ZHAT}
   >>> parse_axis("vertical", basis)
   array([1., 0., 0.])
   >>> parse_axis("-lateral", basis)
   array([ 0.,  0., -1.])


.. py:function:: precision_atol(digits: int | None = None) -> float

   Return an absolute tolerance matching the current display precision.

   ``atol = 0.5 * 10 ** (-digits)`` — half a unit in the last displayed
   decimal place, i.e. the smallest difference that would show up in the
   formatted output.

   :param digits: Number of decimal places.  If None, uses ``get_precision()``.
   :type digits: int or None

   :rtype: float


.. py:function:: psi_trajectory(geometry: ad_hoc_diffractometer.geometry.AdHocDiffractometer, h: float, k: float, l: float, psi_values, *, solution_key=NEAREST_ANGLES) -> list[dict]

   Compute motor-angle sequences for ψ rotation about the scattering vector Q.

   For the fixed reflection (h, k, l) and each target ψ in ``psi_values``,
   finds motor angles that simultaneously satisfy the Bragg condition and
   produce the requested BL1967 operational ψ.

   ψ Definition (Busing & Levy 1967)
   ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
   ψ is the angle through which the sample has rotated about Q relative to
   the base ``forward()`` solution.  ψ = 0 at the base solution by definition.
   Physically, ψ is varied by rotating the sample about Q (changing ω, χ, φ
   or their kappa equivalents) while keeping 2θ fixed.

   This differs from ``geometry.psi()``, which implements the You (1999)
   definition and is constant for all Bragg solutions of a given hkl.  See
   the module docstring for the full discussion.

   :param geometry: Must have ``wavelength`` and ``sample.UB`` set, and an active
                    diffraction mode.  ``kappa_alpha_deg`` must be set for kappa
                    geometries (it is set automatically by the factory functions).
   :type geometry: AdHocDiffractometer
   :param h: Miller indices of the fixed reflection.
   :type h: float
   :param k: Miller indices of the fixed reflection.
   :type k: float
   :param l: Miller indices of the fixed reflection.
   :type l: float
   :param psi_values: Target ψ angles in degrees relative to the base forward() solution.
   :type psi_values: iterable of float
   :param solution_key: Scoring function ``(candidate, previous) -> float``; lower wins.
                        Default: ``NEAREST_ANGLES``.
   :type solution_key: callable or None, optional

   :returns: One entry per ψ value with keys:

             ``"psi_target"`` : float
                 The requested ψ (degrees).
             ``"psi_actual"`` : float or None
                 Achieved ψ verified by internal measurement, or ``None``.
             ``"angles"`` : dict[str, float] or None
                 Motor angles in degrees, or ``None``.
             ``"warning"`` : str or None
                 Descriptive warning when ``angles`` is ``None``, else ``None``.
   :rtype: list of dict

   :raises ValueError: If ``geometry.wavelength`` is None or ``geometry.sample.UB`` is None.
   :raises ValueError: If ``(h, k, l) == (0, 0, 0)``.
   :raises NotImplementedError: If no active diffraction mode is set.

   .. rubric:: Notes

   The geometry must have at least three sample stages (omega/eta, chi/kappa,
   phi/kphi).  Geometries with fewer sample stages are not supported.

   .. rubric:: References

   Busing & Levy, Acta Cryst. 22, 457-464 (1967), angle-setting equations.
   ITC Vol. C, Sec. 2.2.6 (2006), kappa ψ scan.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> g = ahd.fourcv()
   >>> g.wavelength = 1.5406
   >>> g.sample.lattice = ahd.Lattice(a=4.0)
   >>> ahd.ub_identity(g.sample)
   >>> g.mode_name = "bisecting"
   >>> result = ahd.psi_trajectory(g, 1, 1, 0, range(-90, 91, 30))
   >>> for pt in result:
   ...     print(pt["psi_target"], pt["psi_actual"])


.. py:function:: psic(basis: dict = _BASIS_YOU) -> ad_hoc_diffractometer.geometry.AdHocDiffractometer

   You (1999) '4S+2D' six-circle diffractometer (psic geometry).

   Walko (2016) designation: S4D2.
   Default basis: You (1999) — vertical=+x, longitudinal=+y, lateral=+z.
   Detector axis: lateral.  Vertical scattering plane.

   Sample stack (floor first):
       mu  : vertical,     right-handed
       eta : lateral,      left-handed
       chi : longitudinal, right-handed
       phi : lateral,      left-handed

   Detector stack (floor first):
       nu    : vertical, right-handed
       delta : lateral,  left-handed

   mu and nu share the same vertical rotation axis; mechanically independent.

   Reference: H. You, J. Appl. Cryst. 32, 614-623 (1999).
              DOI: 10.1107/S0021889899001223


.. py:function:: q_components(geometry: ad_hoc_diffractometer.geometry.AdHocDiffractometer, angles: dict[str, float] | None = None) -> dict[str, float]

   Decompose Q into components perpendicular and parallel to the sample surface.

   ``Q_perp`` is the component of Q along the surface normal (out-of-plane).
   ``Q_par`` is the magnitude of the component of Q in the surface plane
   (in-plane).

   The signed perpendicular component ``Q_perp_signed`` is positive when Q
   has a component in the same direction as the outward surface normal.

   Units
   -----
   The returned values are in the same units as ``angles_to_phi_vector``,
   i.e. Å⁻¹ using the ``(2π/λ)(kf − ki)`` definition of Q (with full 2π).

   :param geometry: Must have ``wavelength``, ``sample.UB``, and ``surface_normal``
                    (or ``azimuthal_reference``) set.
   :type geometry: AdHocDiffractometer
   :param angles: Motor angles in degrees.  If ``None``, current stage angles are used.
   :type angles: dict[str, float] or None

   :returns:

             ``"Q_perp"`` : float
                 Magnitude of the out-of-plane component (Å⁻¹), always ≥ 0.
             ``"Q_par"`` : float
                 Magnitude of the in-plane component (Å⁻¹), always ≥ 0.
             ``"Q_perp_signed"`` : float
                 Signed out-of-plane component (Å⁻¹); positive when Q points
                 along the outward surface normal.
             ``"Q_total"`` : float
                 Total |Q| (Å⁻¹).
   :rtype: dict with keys

   :raises ValueError: If any required attribute is not set.

   .. rubric:: References

   Lohmeier & Vlieg (1993), §4.2, eq. 17.


.. py:function:: reciprocal_vectors(a1: numpy.ndarray, a2: numpy.ndarray, a3: numpy.ndarray) -> tuple[numpy.ndarray, numpy.ndarray, numpy.ndarray]

   Compute the three reciprocal lattice vectors from Cartesian direct lattice
   vectors.

   Uses the standard crystallographic definition:

       b1 = 2*pi * (a2 x a3) / (a1 . (a2 x a3))
       b2 = 2*pi * (a3 x a1) / (a1 . (a2 x a3))
       b3 = 2*pi * (a1 x a2) / (a1 . (a2 x a3))

   The orthogonality condition is satisfied:

       b_i . a_j = 2*pi * delta_ij

   :param a1: Cartesian direct lattice vectors in Angstroms.
   :type a1: numpy.ndarray, shape (3,)
   :param a2: Cartesian direct lattice vectors in Angstroms.
   :type a2: numpy.ndarray, shape (3,)
   :param a3: Cartesian direct lattice vectors in Angstroms.
   :type a3: numpy.ndarray, shape (3,)

   :returns: **b1, b2, b3** -- Reciprocal lattice vectors in inverse Angstroms (with 2*pi factor).
   :rtype: numpy.ndarray, shape (3,)


.. py:function:: refine_lattice_bl1967(sample, reflections, refine_cell: bool = True, refine_orientation: bool = True, refine_all: bool = False, max_iter: int = 100, tol: float | None = None) -> dict

   Refine lattice parameters and/or orientation from N≥3 reflections.

   Uses the Busing & Levy (1967) least-squares approach (§"Refinement of
   lattice and orientation parameters").  Starting from the current
   ``sample.UB`` and ``sample.lattice``, iteratively solves the linearised
   normal equations to minimise the sum of squared residuals between
   observed and calculated phi-frame scattering vectors.

   Observed vector for reflection i:
       ``hiφ_obs = angles_to_phi_vector(geometry, **ri.angles)``
   Calculated vector:
       ``hiφ_calc = UB @ hi``
   Residual:
       ``Δhiφ = hiφ_obs − hiφ_calc``

   The Jacobian of ``hiφ_calc`` with respect to cell parameters is
   computed by finite difference on the B matrix; the Jacobian with
   respect to the three orientation parameters (infinitesimal rotation
   components) uses the analytical cross-product formula.

   :param sample: The sample whose ``lattice``, ``U``, and ``UB`` are updated
                  in-place.  ``sample.parent`` must be a geometry with
                  ``wavelength`` set (needed by ``angles_to_phi_vector``).
                  ``sample.UB`` must be set (call one of the ``ub_from_*``
                  functions first).
   :type sample: Sample
   :param reflections: N ≥ 3 reflections.  Each may be a ``Reflection`` object or a
                       name string in ``sample.reflections``.
   :type reflections: list of Reflection or str
   :param refine_cell: If True (default), refine the cell parameters.
   :type refine_cell: bool, optional
   :param refine_orientation: If True (default), refine the three orientation parameters
                              (infinitesimal rotation components).
   :type refine_orientation: bool, optional
   :param refine_all: Controls which cell parameters are treated as free:

                      * ``False`` (default) — refine only the parameters that are
                        free in the crystal system of the *current* ``sample.lattice``
                        (e.g. only ``a`` for cubic, ``a`` and ``c`` for tetragonal,
                        etc.).  Symmetry constraints are enforced at every iteration.
                      * ``True`` — treat all six parameters (a, b, c, α, β, γ) as
                        independent, regardless of the starting crystal system.  Use
                        this when the refined cell is expected to break the nominal
                        symmetry, or when the crystal system is unknown.
   :type refine_all: bool, optional
   :param max_iter: Maximum number of iterations (default 100).
   :type max_iter: int, optional
   :param tol: Convergence tolerance on the RMS residual change between
               iterations.  If None, defaults to 1e-10 Å⁻¹.
   :type tol: float or None, optional

   :returns: **result** -- ``lattice``    — refined :class:`Lattice` (also stored on ``sample``)
             ``UB``         — refined UB matrix (3×3 ndarray)
             ``U``          — derived U = UB @ B⁻¹ (3×3 ndarray or None)
             ``residuals``  — per-reflection residual vectors, shape (N, 3) Å⁻¹
             ``rms``        — root-mean-square residual (scalar, Å⁻¹)
             ``converged``  — True if converged within ``max_iter`` iterations
             ``n_iter``     — number of iterations performed
   :rtype: dict

   :raises ValueError: If ``sample.parent`` is None.
   :raises ValueError: If ``sample.UB`` is None (call a ``ub_from_*`` function first).
   :raises ValueError: If fewer than 3 reflections are supplied.
   :raises ValueError: If neither ``refine_cell`` nor ``refine_orientation`` is True.
   :raises TypeError: If any element of ``reflections`` is not a Reflection or str.

   .. rubric:: Notes

   UB is updated in-place at each iteration.  U is derived at the end
   as ``U = UB @ B⁻¹``.  Both ``sample.U`` and ``sample.UB`` are set
   in-place before returning, and ``sample.lattice`` is replaced by
   the refined ``Lattice`` object.

   If the least-squares system is ill-conditioned (e.g. reflections are
   nearly coplanar), the normal equations are solved with
   ``numpy.linalg.lstsq`` (rcond=1e-15), which returns the minimum-norm
   solution.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> g = ahd.fourcv()
   >>> g.wavelength = 1.5406
   >>> g.add_sample("sapphire", ahd.Lattice(a=4.758, c=12.991, gamma=120))
   >>> g.sample = "sapphire"
   >>> # ... add reflections, compute initial UB ...
   >>> result = ahd.refine_lattice_bl1967(
   ...     g.sample, ["r1", "r2", "r3"],
   ...     refine_all=False,   # keep hexagonal constraints
   ... )
   >>> result["converged"]
   True
   >>> result["lattice"].a    # refined a (= b enforced by hexagonal symmetry)
   ...

   .. rubric:: References

   Busing & Levy, Acta Cryst. 22, 457-464 (1967),
   §"Refinement of lattice and orientation parameters".


.. py:function:: refine_lattice_simplex(sample, reflections, refine_cell: bool = True, refine_orientation: bool = False, refine_all: bool = False, max_iter: int = 1000, tol: float | None = None) -> dict

   Derivative-free lattice refinement using the Nelder-Mead simplex method.

   Minimises the scalar cost function::

       f(params) = Σ_i |hiφ_obs_i − UB(params) @ hi|²

   where ``UB(params) = U @ B(params)`` and U is held fixed at its current
   value (unless ``refine_orientation=True``).

   Uses ``scipy.optimize.minimize`` with ``method='Nelder-Mead'`` when
   scipy is available, and falls back to a built-in pure-numpy
   Nelder-Mead implementation otherwise.

   :param sample: The sample whose ``lattice``, ``U``, and ``UB`` are updated
                  in-place.  ``sample.parent`` must be a geometry with
                  ``wavelength`` set.  ``sample.UB`` must already be set.
   :type sample: Sample
   :param reflections: N ≥ 3 reflections.
   :type reflections: list of Reflection or str
   :param refine_cell: If True (default), refine the cell parameters.
   :type refine_cell: bool, optional
   :param refine_orientation: If True, also perturb the orientation parameters (three rotation
                              components) during the simplex search.  Default False.
   :type refine_orientation: bool, optional
   :param refine_all: Controls which cell parameters are treated as free:

                      * ``False`` (default) — refine only the free parameters for the
                        crystal system of the *current* ``sample.lattice``.  Symmetry
                        constraints are enforced by reconstructing a ``Lattice`` at
                        each function evaluation.
                      * ``True`` — treat all six parameters as independent.
   :type refine_all: bool, optional
   :param max_iter: Maximum number of function evaluations (default 1000).
   :type max_iter: int, optional
   :param tol: Convergence tolerance on the cost function value.
               If None, defaults to 1e-12.
   :type tol: float or None, optional

   :returns: **result** -- Same keys as ``refine_lattice_bl1967``:
             ``lattice``, ``UB``, ``U``, ``residuals``, ``rms``,
             ``converged``, ``n_iter``.
   :rtype: dict

   :raises ValueError: If ``sample.parent`` is None or ``sample.UB`` is None.
   :raises ValueError: If fewer than 3 reflections are supplied.
   :raises ValueError: If neither ``refine_cell`` nor ``refine_orientation`` is True.
   :raises TypeError: If any element of ``reflections`` is not a Reflection or str.

   .. rubric:: Notes

   The default ``refine_orientation=False`` leaves U fixed; only the
   cell parameters (and thus B, and thus UB = U @ B) are optimised.
   This is the typical use case: use ``ub_from_two_reflections_bl1967``
   to get U, then use the simplex to find the best cell parameters.

   When scipy is not available, a minimal pure-numpy Nelder-Mead
   implementation is used.

   .. rubric:: Examples

   >>> result = ahd.refine_lattice_simplex(
   ...     g.sample, ["r1", "r2", "r3"],
   ...     refine_all=False,   # keep hexagonal constraints on a=b, gamma=120
   ... )
   >>> result["converged"]
   True

   .. rubric:: References

   Nelder & Mead, The Computer Journal 7(4), 308-313 (1965).


.. py:function:: rotation_matrix(axis: numpy.ndarray, angle_deg: float) -> numpy.ndarray

   Compute a right-handed rotation matrix about a unit axis vector.

   Uses the Rodrigues formula:

       R = I cos(theta) + (1 - cos(theta))(n ⊗ n) + sin(theta)[n]_x

   where [n]_x is the skew-symmetric cross-product matrix of n.

   For a left-handed rotation (e.g. eta, phi, delta in You 1999), pass the
   negated axis: rotation_matrix(-ZHAT, angle_deg).

   The sign of the axis vector encodes handedness:
       +nHat  =>  right-handed rotation about nHat
       -nHat  =>  left-handed rotation about nHat
                  (equivalent to right-handed with negated angle)

   :param axis: Rotation axis vector.  Need not be normalised; it will be normalised
                internally.
   :type axis: numpy.ndarray, shape (3,)
   :param angle_deg: Rotation angle in degrees (right-handed sense about the given axis).
   :type angle_deg: float

   :returns: **R** -- Orthogonal rotation matrix with det(R) = +1.
   :rtype: numpy.ndarray, shape (3, 3)

   .. rubric:: Examples

   >>> import numpy as np
   >>> from ad_hoc_diffractometer.constants import XHAT, ZHAT
   >>> rotation_matrix(XHAT, 0)          # identity
   >>> rotation_matrix(-ZHAT, 30)        # left-handed 30 deg about z


.. py:function:: s2d2(basis: dict = _BASIS_YOU) -> ad_hoc_diffractometer.geometry.AdHocDiffractometer

   S2D2 four-circle diffractometer (general-inclination geometry).

   Walko (2016) designation: S2D2.
   Default basis: You (1999) — vertical=+x, longitudinal=+y, lateral=+z.

   Two independent sample axes (mu, Z) and two independent detector axes
   (nu, delta), all mechanically decoupled.  The angle of incidence is the
   mu angle; the surface normal is parallel to Z.

   Sample stack (floor first):
       mu    : vertical,     right-handed
         --> Z : longitudinal, right-handed

   Detector stack (floor first):
       nu    : vertical,     right-handed
         --> delta : lateral, left-handed

   mu and nu share the same vertical axis; mechanically independent.

   The total scattering angle is (Walko 2016, eq. 18):
       ttheta = arccos(cos(nu) * cos(delta))

   References: K.W. Evans-Lutterodt & M.-T. Tang, J. Appl. Cryst.
                   28, 318-326 (1995).
               D.A. Walko, Ref. Module Mater. Sci. Mater. Eng. (2016), eq. 18.


.. py:function:: set_precision(digits: int) -> None

   Set the package-level display precision.

   :param digits: Number of decimal places for displaying floating-point values.
                  Must be a non-negative integer.
   :type digits: int

   :raises ValueError: If digits is negative or not an integer.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> ahd.set_precision(4)
   >>> ahd.get_precision()
   4


.. py:function:: sixc(basis: dict = _BASIS_YOU) -> ad_hoc_diffractometer.geometry.AdHocDiffractometer

   Lohmeier & Vlieg (1993) six-circle surface diffractometer (sixc geometry).

   Also known as the IUCr six-circle diffractometer.
   Walko (2016) designation: (S3D2)1.
   Default basis: You (1999) — vertical=+x, longitudinal=+y, lateral=+z.

   Sample and detector stacks share the alpha (rotary table) base stage,
   making this a coupled geometry.  Useful for surface diffraction.

   From Fig. 1 and §2.1 of Lohmeier & Vlieg (1993):
       alpha and gamma rotate about the vertical axis (x in LV convention).
       omega, phi, and delta all rotate about the lateral axis (z in LV).
       chi rotates about the longitudinal axis (y in LV).

   Stack (floor first):
       alpha (shared base): vertical,     right-handed  [rotary table]
         --> omega (sample):  lateral,      left-handed
               --> chi:       longitudinal, right-handed
                     --> phi: lateral,      left-handed
         --> delta (detector): lateral,     left-handed
               --> gamma:      vertical,    right-handed

   Reference: M. Lohmeier & E. Vlieg, J. Appl. Cryst. 26, 706-716 (1993).


.. py:function:: trajectory_plan(geometry: ad_hoc_diffractometer.geometry.AdHocDiffractometer, hkl_start: tuple[float, float, float], hkl_end: tuple[float, float, float], n_points: int, *, space: str = 'hkl', solution_key=NEAREST_ANGLES) -> list[dict]

   Plan a motor-angle path between two reciprocal-space points.

   Interpolates between ``hkl_start`` and ``hkl_end`` in ``n_points``
   equally-spaced steps, calls ``geometry.forward()`` at each point, and
   flags portions that are inaccessible (no valid motor solution) or that
   exceed stage limits.

   :param geometry: Must have ``wavelength`` and ``sample.UB`` set and an active mode.
   :type geometry: AdHocDiffractometer
   :param hkl_start: Start and end Miller indices (h, k, l).
   :type hkl_start: tuple of float
   :param hkl_end: Start and end Miller indices (h, k, l).
   :type hkl_end: tuple of float
   :param n_points: Number of evenly-spaced points including endpoints (≥ 2).
   :type n_points: int
   :param space: Interpolation space.

                 ``"hkl"`` (default)
                     Linearly interpolate Miller indices.  Natural for L-scans,
                     H-scans, and crystal-axis-aligned trajectories.  Equal steps in
                     hkl are *not* equal steps in |Q| for non-cubic lattices.

                 ``"Q"``
                     Linearly interpolate in reciprocal Cartesian coordinates
                     (Qx, Qy, Qz = UB @ hkl).  Equal steps in Q regardless of
                     lattice distortion; intermediate hkl values are generally
                     non-integer.  Endpoints are exact by construction.
   :type space: {"hkl", "Q"}, optional
   :param solution_key: Scoring function ``(candidate, previous) -> float``; lower wins.
                        Default: ``NEAREST_ANGLES``.
   :type solution_key: callable or None, optional

   :returns: One entry per trajectory point with keys:

             ``"hkl"`` : tuple of float
                 The (h, k, l) point.
             ``"angles"`` : dict[str, float] or None
                 Motor angles, or ``None`` if inaccessible.
             ``"accessible"`` : bool
                 ``True`` if a valid in-limits solution was found.
             ``"warnings"`` : list of str
                 Empty when accessible; otherwise one or more warning strings.
   :rtype: list of dict

   :raises ValueError: If ``n_points < 2``.
   :raises ValueError: If ``space`` is not ``"hkl"`` or ``"Q"``.
   :raises ValueError: If ``space="Q"`` and ``geometry.sample.UB`` is None or singular.
   :raises ValueError: If ``geometry.wavelength`` is None or ``geometry.sample.UB`` is None.
   :raises NotImplementedError: If no active diffraction mode is set.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> g = ahd.fourcv()
   >>> g.wavelength = 1.5406
   >>> g.sample.lattice = ahd.Lattice(a=4.0)
   >>> ahd.ub_identity(g.sample)
   >>> g.mode_name = "bisecting"
   >>> plan = ahd.trajectory_plan(g, (1, 0, 0), (3, 0, 0), n_points=11)
   >>> accessible = [pt for pt in plan if pt["accessible"]]
   >>> print(f"{len(accessible)}/{len(plan)} points accessible")


.. py:function:: two_theta_to_Q_mag(two_theta_deg: float, wavelength: float) -> float

   Convert 2θ to |Q| using the Bragg relation.

   |**Q**| = 4π sin(θ) / λ

   :param two_theta_deg: Diffraction angle 2θ in degrees.
   :type two_theta_deg: float
   :param wavelength: Wavelength in Å.
   :type wavelength: float

   :returns: |Q| in Å⁻¹.
   :rtype: float

   :raises ValueError: If ``two_theta_deg`` is not in (0°, 180°) or ``wavelength`` ≤ 0.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> round(ahd.two_theta_to_Q_mag(30.0, 1.5406), 4)
   2.0904


.. py:function:: two_theta_to_d(two_theta_deg: float, wavelength: float) -> float

   Convert 2θ to d-spacing using Bragg's law.

   λ = 2 d sin(θ)  →  d = λ / (2 sin(θ))

   :param two_theta_deg: Diffraction angle 2θ in degrees.
   :type two_theta_deg: float
   :param wavelength: Wavelength in Å.
   :type wavelength: float

   :returns: d-spacing in Å.
   :rtype: float

   :raises ValueError: If ``two_theta_deg`` is not in (0°, 180°) or ``wavelength`` ≤ 0.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> round(ahd.two_theta_to_d(22.6956, 1.5406), 4)
   2.0


.. py:function:: ub_from_one_reflection(sample, reflection, reference_hkl: tuple[float, float, float] = (0.0, 0.0, 1.0), reference_stage=None) -> numpy.ndarray

   Compute a provisional U and UB from one reflection (Rodrigues method).

   A common first step in a diffractometer alignment session: assume that
   a known high-symmetry crystal direction (``reference_hkl``) is parallel
   to a specific diffractometer axis (``reference_stage``).  This gives a
   "fake" UB sufficient to predict where to scan for a second reflection.

   The algorithm:

   1. Compute the crystal-frame direction: ``Bh = B @ reference_hkl``
      (normalised to ``Bh_hat``).
   2. Extract the lab-frame direction from ``reference_stage`` (normalised
      to ``r_hat``).
   3. Find the minimal rotation (Rodrigues) that takes ``Bh_hat`` to
      ``r_hat``:
      ``axis  = cross(Bh_hat, r_hat)``
      ``angle = arccos(clip(dot(Bh_hat, r_hat), -1, 1))``
      ``U     = rotation_matrix(axis, degrees(angle))``
   4. ``UB = U @ B``
   5. Store ``sample.U = U`` and ``sample.UB = UB``; return ``UB``.

   Edge cases:

   - **Parallel** (``angle ≈ 0``): ``Bh_hat`` already points along
     ``r_hat``; ``U = I``.
   - **Anti-parallel** (``angle ≈ π``): choose an arbitrary perpendicular
     axis (the first vector from ``[XHAT, YHAT, ZHAT]`` not parallel to
     ``Bh_hat``); rotate 180° about it.

   .. note::
      The result is approximate.  If ``reference_hkl`` is not truly
      parallel to ``reference_stage.axis`` (e.g. χ = 89.32° rather than
      90.00°), predicted angles for subsequent reflections will be slightly
      wrong.  Refine with ``ub_from_two_reflections_bl1967()`` once a
      second reflection is measured.

   :param sample: The sample whose ``U`` and ``UB`` attributes are updated in-place.
                  ``sample.lattice`` must be set.  If ``sample.parent`` is set, it
                  is used to resolve a string ``reference_stage``.
   :type sample: Sample
   :param reflection: A ``Reflection`` object or the name of a reflection in
                      ``sample.reflections``.
   :type reflection: Reflection or str
   :param reference_hkl: Miller indices of the crystal direction assumed to be aligned with
                         ``reference_stage``.  Default ``(0, 0, 1)`` (c-axis).
   :type reference_hkl: tuple of float, optional
   :param reference_stage: The diffractometer axis assumed to be parallel to ``reference_hkl``.

                           - ``Stage`` object: ``stage.axis`` is used directly (recommended;
                             the sign convention is already encoded in the Stage).
                           - ``str``: looked up as ``sample.parent.stage(name)``.
                           - ``None`` and ``sample.parent`` is set: defaults to
                             ``sample.parent.stage("phi")``.
                           - ``None`` and ``sample.parent`` is ``None``: raises ``ValueError``.
   :type reference_stage: Stage, str, or None, optional

   :returns: **UB** -- Sets ``sample.U`` and ``sample.UB`` in-place before returning.
   :rtype: numpy.ndarray, shape (3, 3)

   :raises KeyError: If ``reflection`` is a string not found in ``sample.reflections``.
   :raises ValueError: If ``reference_stage`` cannot be resolved (no parent, no stage).
   :raises ValueError: If ``reference_hkl`` maps to the zero vector under B.

   .. rubric:: Examples

   >>> g = psic()
   >>> g.add_sample("sapphire", Lattice(a=4.758, c=12.991))
   >>> g.sample = "sapphire"
   >>> g.add_reflection("r1", hkl=(0, 0, 6),
   ...                  angles={"mu": 0, "eta": 20.97, "chi": 90,
   ...                          "phi": 0, "nu": 0, "delta": 41.94})
   >>> g.sample.reflections.setor1("r1")
   >>> UB = ub_from_one_reflection(
   ...     g.sample, "r1",
   ...     reference_hkl=(0, 0, 1),
   ...     reference_stage=g.stage("phi"),
   ... )


.. py:function:: ub_from_three_reflections_bl1967(sample, r1, r2, r3) -> numpy.ndarray

   Compute UB directly from three reflections (Busing & Levy 1967, eqs. 29-31).

   This method requires no prior knowledge of the lattice: it computes UB
   directly by matrix inversion from three measured reflections.  If a
   lattice B matrix is available on the sample, U is also derived.

   Algorithm (BL1967 eqs. 28-31):

   1. For each reflection i compute the phi-frame scattering vector
      ``hiφ = angles_to_phi_vector(geometry, **ri.angles)`` (eq. 28 gives
      the magnitude; ``angles_to_phi_vector`` already carries the full
      vector in Å⁻¹).
   2. Stack as column matrices::

          Hφ = [h1φ | h2φ | h3φ]    (3×3, columns are phi-frame vectors)
          H  = [h1  | h2  | h3 ]    (3×3, columns are Miller-index triples)

   3. ``UB = Hφ @ inv(H)``  (eq. 31).
   4. If ``sample.lattice`` is set: ``U = UB @ inv(B)`` (derived from UB).
   5. Store ``sample.UB = UB`` (first) and ``sample.U = U``; return ``UB``.

   :param sample: The sample whose ``UB`` (and ``U``) attributes are updated in-place.
                  ``sample.parent`` must be a geometry with ``wavelength`` set.
   :type sample: Sample
   :param r1: Three orienting reflections.  Each may be a ``Reflection`` object or
              the name of a reflection in ``sample.reflections``.
   :type r1: Reflection or str
   :param r2: Three orienting reflections.  Each may be a ``Reflection`` object or
              the name of a reflection in ``sample.reflections``.
   :type r2: Reflection or str
   :param r3: Three orienting reflections.  Each may be a ``Reflection`` object or
              the name of a reflection in ``sample.reflections``.
   :type r3: Reflection or str

   :returns: **UB** -- ``sample.UB`` is set first (directly, via eq. 31), then
             ``sample.U = UB @ inv(B)`` is derived.  Both are set in-place.
   :rtype: numpy.ndarray, shape (3, 3)

   :raises KeyError: If any of ``r1``, ``r2``, ``r3`` is a string not found in
       ``sample.reflections``.
   :raises TypeError: If any argument is not a ``Reflection`` or a string.
   :raises ValueError: If ``sample.parent`` is ``None``.
   :raises ValueError: If the three Miller-index column matrix ``H`` is singular
       (``|det(H)| < tol``), i.e. the three hkl vectors are coplanar.
   :raises ValueError: If ``sample.parent.wavelength`` is ``None``.

   :Warns: **UserWarning** -- If ``det(H) < 0``, the hkl triples form a left-handed system.
           The computation proceeds but the sign convention may give U with
           ``det(U) = -1``; consider swapping r1 and r2 to make det(H) > 0.

   .. rubric:: Notes

   UB is computed first (``sample.UB = Hφ @ H⁻¹``).  U is then derived
   as ``sample.U = UB @ B⁻¹``.  This is the opposite order from
   ``ub_from_two_reflections_bl1967``, where U is computed first.

   The method does not require a known lattice: ``H`` is formed from the
   raw hkl indices, not from ``B @ hkl``.  However, if ``sample.lattice``
   is the package default (cubic, a = 1 Å) rather than a measured lattice,
   the derived U will not be physically meaningful.

   If ``det(H)`` is exactly zero (degenerate reflections), ``numpy.linalg.inv``
   will raise ``LinAlgError``, which is caught and re-raised as ``ValueError``.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> import math
   >>> g = ahd.psic()
   >>> g.wavelength = 2 * math.pi
   >>> g.sample.lattice = ahd.Lattice(a=2 * math.pi)
   >>> g.add_reflection("r1", hkl=(1, 0, 0),
   ...     angles={"mu": 0, "eta": 30, "chi": 0, "phi": 0, "nu": 0, "delta": 60})
   >>> g.add_reflection("r2", hkl=(0, 1, 0),
   ...     angles={"mu": 0, "eta": 30, "chi": 0, "phi": 90, "nu": 0, "delta": 60})
   >>> g.add_reflection("r3", hkl=(0, 0, 1),
   ...     angles={"mu": 0, "eta": 30, "chi": 90, "phi": 30, "nu": 0, "delta": 60})
   >>> UB = ahd.ub_from_three_reflections_bl1967(g.sample, "r1", "r2", "r3")

   .. rubric:: References

   Busing & Levy, Acta Cryst. 22, 457-464 (1967), eqs. 28-31.


.. py:function:: ub_from_two_reflections_bl1967(sample, r1=None, r2=None) -> numpy.ndarray

   Compute U and UB from two orienting reflections (Busing & Levy 1967, eqs. 23-27).

   Given two reflections with known hkl and measured motor angles, and a
   known lattice (B matrix), this function computes the orientation matrix U
   and then UB = U @ B, storing both on the sample.

   Algorithm (BL1967 eqs. 23-27):

   1. For each reflection, call ``angles_to_phi_vector()`` to get the
      scattering vector in the phi frame: ``u1φ``, ``u2φ``.
   2. From hkl and the lattice B matrix: ``h1c = B @ h1``, ``h2c = B @ h2``.
   3. Build orthonormal triple ``Tc`` in the crystal frame via Gram-Schmidt:
      ``t1c ∥ h1c``, ``t2c`` in the plane of ``h1c`` and ``h2c``,
      ``t3c = t1c × t2c``.
   4. Build the matching triple ``Tφ`` in the phi frame from ``u1φ``, ``u2φ``.
   5. Compute ``U = Tφ @ Tc.T``  (eq. 27; Tc is orthogonal so Tc⁻¹ = Tc.T).
   6. Compute ``UB = U @ B``.
   7. Store ``sample.U = U``, ``sample.UB = UB``; return ``UB``.

   :param sample: The sample whose ``U`` and ``UB`` attributes are updated in-place.
                  ``sample.lattice`` must be set.  ``sample.parent`` must be a geometry
                  with ``wavelength`` set (it is used to call ``angles_to_phi_vector``).
   :type sample: Sample
   :param r1: Primary orienting reflection.  If ``None``, defaults to
              ``sample.reflections.orienting_reflections[0]``.
   :type r1: Reflection, str, or None
   :param r2: Secondary orienting reflection.  If ``None``, defaults to
              ``sample.reflections.orienting_reflections[1]``.
   :type r2: Reflection, str, or None

   :returns: **UB** -- Sets ``sample.U`` (first) and ``sample.UB = sample.U @ B`` in-place
             before returning.
   :rtype: numpy.ndarray, shape (3, 3)

   :raises KeyError: If ``r1`` or ``r2`` is a string not found in ``sample.reflections``.
   :raises ValueError: If ``r1`` or ``r2`` is ``None`` and the required orienting reflection
       has not been designated (``setor1``/``setor2`` not called).
   :raises ValueError: If ``sample.parent`` is ``None`` (needed to call
       ``angles_to_phi_vector``).
   :raises ValueError: If ``sample.parent.wavelength`` is ``None``.
   :raises ValueError: If the two reflections are parallel in the crystal frame (h1c and
       h2c collinear) or in the phi frame (u1φ and u2φ collinear).
   :raises TypeError: If ``r1`` or ``r2`` is not a ``Reflection``, string, or ``None``.

   .. rubric:: Notes

   U is computed first (``sample.U = Tφ @ Tc.T``), then UB is derived from
   it (``sample.UB = sample.U @ B``).

   The wavelength used for ``angles_to_phi_vector`` is taken from
   ``sample.parent.wavelength``.  If a reflection carries its own
   ``wavelength`` attribute, that is *not* used here; the geometry's
   wavelength governs the conversion from motor angles to Q_phi.

   .. rubric:: References

   Busing & Levy, Acta Cryst. 22, 457-464 (1967), eqs. 23-27.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> g = ahd.psic()
   >>> g.wavelength = 1.5406
   >>> g.add_sample("sapphire", ahd.Lattice(a=4.758, c=12.991))
   >>> g.sample = "sapphire"
   >>> g.add_reflection("r1", hkl=(0, 0, 6),
   ...     angles={"mu": 0, "eta": 20.97, "chi": 90, "phi": 0,
   ...             "nu": 0, "delta": 41.94})
   >>> g.add_reflection("r2", hkl=(1, 0, 4),
   ...     angles={"mu": 0, "eta": 23.72, "chi": 57.04, "phi": 0,
   ...             "nu": 0, "delta": 48.13})
   >>> g.sample.reflections.setor1("r1")
   >>> g.sample.reflections.setor2("r2")
   >>> UB = ahd.ub_from_two_reflections_bl1967(g.sample)


.. py:function:: ub_identity(sample) -> numpy.ndarray

   Set U = I (identity) and UB = B; return UB.

   The crudest orientation assumption: crystal axes are aligned with the
   lab axes.  Useful as a starting point when no reflection information
   is available at all.

   :param sample: The sample whose ``U`` and ``UB`` attributes are updated in-place.
   :type sample: Sample

   :returns: **UB**
   :rtype: numpy.ndarray, shape (3, 3)


.. py:function:: wavelength_to_energy(wavelength: float) -> float

   Convert wavelength to photon energy for X-rays.

   E (keV) = hc / λ = 12.39842 / λ (Å)

   :param wavelength: Wavelength in Å.
   :type wavelength: float

   :returns: Photon energy in keV.
   :rtype: float

   :raises ValueError: If ``wavelength`` ≤ 0.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> round(ahd.wavelength_to_energy(1.5406), 4)
   8.0478


.. py:function:: wavelength_to_wavenumber(wavelength: float) -> float

   Convert wavelength to wave number k.

   k (Å⁻¹) = 2π / λ (Å)

   :param wavelength: Wavelength in Å.
   :type wavelength: float

   :returns: Wave number k in Å⁻¹.
   :rtype: float

   :raises ValueError: If ``wavelength`` ≤ 0.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> round(ahd.wavelength_to_wavenumber(1.0), 6)
   6.283185


.. py:function:: wavenumber_to_wavelength(wavenumber: float) -> float

   Convert wave number k to wavelength.

   λ (Å) = 2π / k (Å⁻¹)

   :param wavenumber: Wave number k in Å⁻¹.
   :type wavenumber: float

   :returns: Wavelength in Å.
   :rtype: float

   :raises ValueError: If ``wavenumber`` ≤ 0.

   .. rubric:: Examples

   >>> import ad_hoc_diffractometer as ahd
   >>> round(ahd.wavenumber_to_wavelength(6.283185), 6)
   1.0


.. py:function:: zaxis(basis: dict = _BASIS_YOU) -> ad_hoc_diffractometer.geometry.AdHocDiffractometer

   Z-axis four-circle diffractometer (general-inclination geometry).

   Walko (2016) designation: (S1D2)1.
   Default basis: You (1999) — vertical=+x, longitudinal=+y, lateral=+z.

   Designed for surface diffraction.  The sample surface normal is aligned
   parallel to the Z-axis, so the angle of incidence equals the alpha angle.
   The detector and sample both rotate about the shared alpha (base) axis.

   Stack (floor first):
       alpha (shared base): vertical,     right-handed
         --> Z     (sample)  : longitudinal, right-handed
         --> delta (detector): lateral,      left-handed
               --> gamma :     vertical,     right-handed

   The total scattering angle is a compound of gamma, delta, and alpha
   (Walko 2016, eq. 17):
       ttheta = arccos(cos(gamma)*cos(delta)*cos(alpha) + sin(alpha)*sin(gamma))

   References: J.M. Bloch, J. Appl. Cryst. 18, 33-36 (1985).
               D.A. Walko, Ref. Module Mater. Sci. Mater. Eng. (2016), eq. 17.


